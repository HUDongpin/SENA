# SENA.HK Research Pilot Delivery Candidate

SENA — Social-Epistemic Nexus Analytics — is a local research-pilot delivery candidate for combining social-network analysis, epistemic-network analysis, and traceable social-epistemic bridge evidence.

Current status: local research pilot delivery candidate with a runnable enterprise readiness loop, not a completed production SaaS cutover. The intended handoff is still a stable researcher walkthrough and reproducibility-review package, while the local enterprise runtime makes auth, RBAC teams, server-side project persistence, import adapters, reliability workflows, group-comparison validation suites, notification outbox plus webhook delivery, signed collaboration pub/sub bridge delivery, signed managed-database sync bridge delivery, signed audit/SIEM, backup/object-storage, alerting, email, IdP/provisioning bridge evidence, native adapter certification, SaaS operations readiness records, release-gate review records, and go-live rehearsal records runnable before institution cutover. A clean handoff requires the full local verification gate to pass after any local `next dev` or `next start` server has been stopped.

The core demo route is `/workspace/sena`. It builds:

- `S`: person-person social interaction matrix
- `W`: code-code epistemic co-occurrence matrix
- `B` / `B_PC`: person-to-code contribution matrix (`B` is the compatibility alias)
- `B_CP`: code-to-person bridge matrix, independently estimated from valid target-person evidence or set to the transpose-compatible `B_PC` fallback
- `G`: person-code-pair contribution matrix
- `A_fusion`: weighted typed matrix `[alpha*S gamma*B_PC; gamma*B_CP beta*W]`; independent `B_CP` evidence activates directed bridge semantics
- jSNA social-network manifest and metrics through local `sna.js`, including actor centrality, graph connectivity, communities, components, and S matrix
- jENA manifest and standalone ENA report outputs through local `jena-js`, including projected unit points, code node positions, connection counts, line weights, and W-matrix handoff evidence
- jENA/jSNA/SENA runtime bundle export with data-contract audit, SENA method protocol, visual grammar artifact, development plan, pilot package manifest, production page contract, runtime consistency checks, standalone jENA ENA report, jENA concept-pair handoff checks against the SENA `W` matrix, artifact-level evidence coverage for jENA/jSNA/SENA handoff files, fusion math audit, demo walkthrough, demo verification checklist and compatibility audit, pilot-readiness checks, standalone coding-reliability gate, claim-readiness gate, temporal runtime trace with per-window matrix fingerprints, and single-file review packet with its own completeness audit for reproducibility review
- temporal windows with per-window jENA/jSNA runtime trace, S/W/B/B_PC/B_CP/G/A_fusion matrix fingerprints, visible A_fusion checksums, adjacent-window S/W/B/B_PC/B_CP/G/A_fusion transition evidence, Temporal Fusion Arc S/W/B/G story view, evidence snippets, evidence ledger export, and JSON/Markdown reports with data-contract, completeness, and formula audits
- data-contract audit export, runtime provenance, five-table evidence lineage, and interpretation guardrails in the workspace, with formula-audit records retained in report, runtime bundle, and review-packet archives

This milestone is local research pilot delivery plus a file-backed enterprise runtime. It now includes runnable local auth with failed-login audit, temporary lockout, auth/API rate limiting, TOTP MFA, and password reset tokens, configurable OAuth/OIDC SSO for institution, Google, or ORCID providers with a local pilot fallback, service-token organization provisioning and SCIM 2.0 Users/Groups bridge sync for teams/users/SSO identities/memberships, RBAC memberships, saved projects with revision history, append-only revision restore, and optimistic `currentVersion`/`expectedVersion` conflict protection, team invitation create/accept/revoke and membership lifecycle controls, local in-app notification outbox delivery with a configurable webhook delivery/retry worker, signed institution email webhook delivery for password-reset and team-invitation messages with sealed queued payloads, project comments, live collaboration SSE stream with presence heartbeat, signed collaboration pub/sub event queue delivery for presence/comments/comment resolution/adjudication, adjudication records, upload security/DLP scan status with signed managed object-storage delivery, persisted import-run history with data-quality lineage and `sena-import-cleaning-manifest/v1` cleaning manifests, LMS/forum JSON plus CSV/XLSX discussion-export normalization and TXT/MD/SRT/VTT transcript cleaning into SENA people/utterances/reply interactions/coded segments/codebook tables, persisted server-side SENA analysis run history with artifact SHA-256 fingerprints, persisted reliability-run history with reviewer sign-off and batch adjudication generation from disagreement queues, persisted group-comparison validation runs, preregistration plan SHA-256 fingerprints, validation parity evidence manifests for rENA/R sna fixture coverage plus walkthrough/expert-review/inference gates, and Holm-corrected multi-metric suites with reviewer approval, persisted domain expert review sign-off with claim-scope/rating evidence, enterprise import/reliability/validation/export APIs, governance health checks, ops status/metrics/readiness endpoints plus signed alert webhook delivery and a redacted organization deployment handoff package for deployment monitoring and production-gate evidence, filtered audit-log JSON/CSV export with signed audit/SIEM webhook forwarding, signed managed-database sync bridge delivery for sanitized enterprise state, team-scoped backup export plus signed managed-backup webhook delivery and dry-run/merge restore rehearsal, file-backed write locking with write-before backup recovery, deployment security response headers including HSTS, COOP, CORP, CSP report-only, frame, referrer, permissions, and nosniff controls, and audit logging. Production identity-provider approval, managed database/object-storage/pub-sub/audit-SIEM/backup/email/alerting ownership, full SaaS backend operating-model approval, release-gate verification, and go-live attestation are now represented as machine-readable adapter certification, platform-decision acceptance, SaaS-operations readiness, release-gate, and go-live rehearsal records; external institution credential issuance and staffed operations are performed by the owning institution outside this repository.

## Delivery Candidate Scope

The four-week delivery plan is encoded in exported `sena-development-plan.json` and archived inside `sena-runtime-bundle.json` and `sena-review-packet.json`. It keeps the next development phase focused on:

- Week 1: freeze the pilot baseline and verify local jENA/jSNA/SENA runtime provenance.
- Week 2: polish the webENA-inspired researcher workbench without changing routes or v1 export schemas.
- Week 3: strengthen coding-reliability, human-review, and claim-readiness gates.
- Week 4: package the fixed handoff with sample data, templates, project snapshot, runtime bundle, review packet, verification checklist, walkthrough, and bilingual demo script.

The fixed pilot handoff package is:

- `public/sena-pilot/sample/lesson-study-sena-contract.json` and the five lesson-study CSV files
- `public/sena-pilot/templates/*` blank templates
- `sena-project-snapshot.json`
- `sena-runtime-bundle.json`
- `sena-review-packet.json`
- `sena-demo-verification.json`
- `sena-demo-walkthrough.json`
- `sena-development-plan.json`

Demo script: import data -> review Fusion Canvas -> inspect Temporal Trace -> inspect evidence -> export review packet.

## Next Stage Development Plan

The next development phase starts from the verified local research pilot delivery candidate rather than a fresh POC. The generated `sena-development-plan.json` now carries a `nextStage` block with four decision-ordered phases:

- `Pilot Handoff Freeze`: freeze the six-step `/workspace/sena` workflow and fixed handoff package before external walkthroughs.
- `Researcher Walkthrough`: run the lesson-study sample and 1-2 real research datasets through import, Temporal Fusion Arc, G contribution, evidence inspection, and review-packet export.
- `Research Validation`: expand jENA/rENA and jSNA/R sna parity evidence, coding-reliability evidence, uncertainty/stability notes, and domain expert review.
- `Production Platform Acceptance`: record whether each production backend adapter is accepted as a signed bridge or replaced with an institution-native service, including production auth, managed persistence, audit-log retention, signed audit/SIEM forwarding, signed backup/restore delivery, signed deployment alert delivery/escalation ownership, institution email delivery, and institution SCIM/IdP bridge ownership.

Release gate: run `npm run sena:pilot:verify` before every handoff. Claims remain `exploratory-only` until human review and coding-reliability gates pass. The local enterprise runtime is suitable for pilot walkthroughs and emits redacted organization deployment evidence, native adapter certification, SaaS operations readiness records, release-gate review records, go-live rehearsal records, rollback drill evidence, and post-cutover monitor artifacts. These records are cutover evidence, not proof that production SaaS operation is complete. Production identity-provider approval/secrets, native managed database/object-storage/pub-sub/audit-SIEM/backup-restore adapter ownership, SaaS backend operations, and institution-level deployment ownership must be closed through platform-owner acceptance records or native-ready adapter evidence before cutover.

 Identity cutover evidence uses canonical platform-decision IDs so the request packet, capability audit, release gate, and go-live rehearsal all report the same blockers. IdP production evidence IDs: `idp-tenant-approval`, `idp-callback-approval`, `sso-provider-secrets`, `sso-secret-store-reference`, and `sso-secret-rotation`. Provisioning production evidence IDs: `provisioning-owner`, `scim-or-idp-ownership`, `bearer-token-rotation`, and `lifecycle-guardrails`. Evidence URLs must be institution-owned HTTPS locations, while tenant IDs, secret-store references, and rotation versions are stored only as binding hashes.

## Stack

- Next.js App Router
- React + TypeScript
- Tailwind CSS
- Framer Motion
- lucide-react icons
- Local `jena-js` ENA runtime from `vendor/jena-js`
- Local `sna.js` SNA runtime from `vendor/sna-js`
- Browser-side SENA model construction in `lib/sena`
- File-backed local enterprise runtime in `.sena-enterprise/enterprise-db.json` for auth, failed-login lockout state, auth/API rate-limit buckets, TOTP MFA factors/setup/challenges, password-reset token state, OAuth/OIDC SSO state, service-token organization provisioning metadata, SCIM 2.0 Users/Groups bridge metadata, RBAC, saved projects, revision history, append-only revision restore, optimistic project-version conflict protection, upload registry with SHA-256 source-file lineage, local security/DLP scan status, upload blob integrity verification, and signed object-storage delivery, import-run history with adapter profiles, dataset counts, warning previews, cleaning manifests, and upload IDs, server-side SENA analysis run history with report/snapshot/runtime-bundle fingerprints, project comments, local in-app notification outbox delivery with webhook delivery status, sealed email delivery queue status for password resets and invitations, live collaboration SSE stream, presence heartbeat, signed collaboration pub/sub delivery queue for presence/comment/comment-resolution/adjudication events, adjudication records, reliability-run history with Cohen's kappa/Krippendorff alpha dashboards and reviewer sign-off status, group-comparison validation run history with permutation p values, bootstrap confidence intervals, effect sizes, Holm-corrected suite summaries, preregistration plan hashes, preregistration/method notes, approval status, domain expert review status, claim scope, data/method/interpretation ratings, team invitation create/accept/revoke, membership role/status lifecycle controls, import/reliability/validation/export APIs, governance checks, filtered audit-log JSON/CSV export, signed audit webhook forwarding state for SIEM/logging, signed managed-database sync delivery for sanitized enterprise-state snapshots, team-scoped backup export, backup verification, signed backup webhook delivery to managed storage/database bridges, dry-run/merge restore rehearsal, write-lock and `.bak` write-before backup probes, and audit logs

## Module Boundaries

- Schema contract names live in `lib/sena/schema-registry.ts`; new touched code should prefer `SENA_SCHEMA_VERSIONS` over scattered `schemaVersion` literals.
- Workspace API routes live in `components/sena/workspace/api-client.ts`; `SenaFusionWorkspace.tsx` should not add direct `/api/...` fetch literals.
- Enterprise routes should import from `lib/sena/enterprise/*` domain modules. `lib/sena/enterprise.ts` remains a compatibility facade while the monolith is split.
- Enterprise persistence is represented by `SenaEnterpriseStateStore` in `lib/sena/enterprise/state.ts`; the current default adapter remains the file-backed local JSON store.

## Run Locally

```bash
npm install
npm run dev
```

Open:

```text
http://localhost:3000/workspace/sena
```

### Enterprise SSO Configuration

The `/login` and `/register` SSO buttons use `/api/auth/sso`. The pages query `/api/auth/sso?status=1`; configured providers start a real OAuth/OIDC authorization-code flow with PKCE through the GET redirect endpoint and return through `/api/auth/sso/callback`, while providers without secrets keep the local pilot fallback so walkthroughs remain runnable. In `NODE_ENV=production`, that local fallback is disabled by `sena-enterprise-sso-fallback-policy/v1` unless `SENA_ALLOW_LOCAL_SSO_FALLBACK=1` is set for an explicitly approved pilot-only deployment. Registration links such as `/register?inviteCode=...` prefill the invitation code, and SSO state carries that code through callback so an invited Google, ORCID, or institution SSO user joins the invited team when the provider email matches the invitation. Deployment operators can call `/api/auth/sso?status=1&preflight=1` to run `sena-enterprise-sso-preflight/v1`, which verifies provider configuration, resolves discovery or explicit endpoints, checks callback URL/scopes, stores redacted preflight audit evidence, and feeds the production readiness `oidc-provider` gate.

Configure one or more providers with:

```bash
SENA_APP_URL=https://your-sena-host.example
SENA_SSO_GOOGLE_CLIENT_ID=...
SENA_SSO_GOOGLE_CLIENT_SECRET=...
# optional, defaults to Google's OIDC discovery URL
SENA_SSO_GOOGLE_DISCOVERY_URL=...

SENA_SSO_ORCID_CLIENT_ID=...
SENA_SSO_ORCID_CLIENT_SECRET=...
# optional, defaults to ORCID's OIDC discovery URL
SENA_SSO_ORCID_DISCOVERY_URL=...

SENA_SSO_INSTITUTION_CLIENT_ID=...
SENA_SSO_INSTITUTION_TENANT_ID=...
SENA_SSO_INSTITUTION_CLIENT_SECRET=...
SENA_SSO_INSTITUTION_CLIENT_SECRET_REF=institution-vault/sena/sso-client-secret
SENA_SSO_INSTITUTION_CLIENT_SECRET_VERSION=sso-client-secret-rotation-YYYY-MM
SENA_IDENTITY_SECRET_ROTATION_CADENCE_DAYS=180
SENA_SSO_INSTITUTION_DISCOVERY_URL=https://idp.example.edu/.well-known/openid-configuration
```

For providers without discovery metadata, set `SENA_SSO_<PROVIDER>_ISSUER`, `SENA_SSO_<PROVIDER>_AUTHORIZATION_URL`, `SENA_SSO_<PROVIDER>_TOKEN_URL`, `SENA_SSO_<PROVIDER>_USERINFO_URL`, and `SENA_SSO_<PROVIDER>_JWKS_URL`. SENA validates signed OIDC `id_token` values against JWKS plus issuer, audience, nonce, expiry, and issued-at claims before accepting the SSO callback. Check `/api/auth/sso?status=1` or `/api/sena/governance/health` to see which providers are configured, which environment variables are still missing, and whether the local SSO fallback policy is enabled. Run `/api/auth/sso?status=1&preflight=1&provider=google` or the matching provider before handoff; governance records `auth.sso.preflight.pass/fail` without storing client secrets. In production, set non-secret `SENA_SSO_INSTITUTION_TENANT_ID` so tenant approval evidence is bound to the institution IdP tenant or app registration, set `SENA_IDENTITY_SECRET_ROTATION_CADENCE_DAYS` to the institution-approved SSO/provisioning rotation cadence from 1 to 180 days, and pair each configured identity secret with a non-secret secret-store reference such as `SENA_SSO_INSTITUTION_CLIENT_SECRET_REF` plus a non-secret rotation version such as `SENA_SSO_INSTITUTION_CLIENT_SECRET_VERSION`; readiness stores only hashes and never stores or exports the secret value.

Password login has local enterprise brute-force protection. Defaults are 5 failed attempts in a 15-minute window followed by a 15-minute lockout; override with `SENA_AUTH_LOCKOUT_MAX_FAILURES`, `SENA_AUTH_LOCKOUT_WINDOW_MINUTES`, and `SENA_AUTH_LOCKOUT_MINUTES`. Registration and password-reset confirmation enforce `sena-enterprise-password-policy/v1`: at least 12 characters, letters and numbers, no common weak-password fragments, and no email local-part reuse. Audit entries use `auth.login.failed` and `auth.login.locked` with email hashes and domains rather than raw email addresses.

Auth endpoints also use file-backed local API rate limiting. Defaults are `SENA_AUTH_API_RATE_LIMIT_MAX_REQUESTS=20` per `SENA_AUTH_API_RATE_LIMIT_WINDOW_SECONDS=60`, password reset defaults to `SENA_PASSWORD_RESET_RATE_LIMIT_MAX_REQUESTS=5` per `SENA_PASSWORD_RESET_RATE_LIMIT_WINDOW_SECONDS=900`, and SSO defaults to `SENA_SSO_RATE_LIMIT_MAX_REQUESTS=30` per `SENA_SSO_RATE_LIMIT_WINDOW_SECONDS=300`. Rate-limit keys are SHA-256 hashed before storage, over-limit events audit as `security.rate_limit`, and `apiRateLimits` are excluded from team backups as transient security state.

Cookie-auth mutating endpoints require `/api/auth/csrf`; send the returned token in `x-sena-csrf-token` for session-authenticated `POST`, `PUT`, `PATCH`, and `DELETE` requests. Bearer-token provisioning, SCIM, and ops service calls do not use CSRF. Failures audit as `security.csrf.fail`, and tokens are bound to the current session using `SENA_CSRF_SECRET` or `SENA_SESSION_SECRET`.

Authenticator MFA is available through the `/workspace/sena` Enterprise runtime Account security panel and `/api/auth/mfa`. Authenticated users can start setup, enter the current TOTP code to enable MFA, check status, and disable MFA from the workbench; API clients can `POST {"action":"setup"}` to receive a one-time TOTP setup secret and `otpauth://` URL, `POST {"action":"enable","setupToken":"...","code":"123456"}` to enable MFA, `GET` to check status, and `DELETE {"code":"123456"}` to disable it. MFA login returns `202` from `/api/auth/login` with a challenge token until a valid TOTP code is submitted. MFA secrets are sealed with AES-256-GCM; set `SENA_MFA_ENCRYPTION_KEY` or `SENA_SESSION_SECRET` in production-like environments so governance health does not rely on the local fallback key.

Claim-authorizing domain-expert approvals require a server-authenticated full-record receipt. Configure both a dedicated, non-placeholder `SENA_EXPERT_REVIEW_SIGNING_SECRET` of at least 32 characters and an explicit opaque `SENA_EXPERT_REVIEW_SIGNING_KEY_ID`. On every secret rotation, issue a new key id and provide still-trusted prior keys only through `SENA_EXPERT_REVIEW_VERIFICATION_KEYS_JSON` as an object from opaque key ids to secret values. Reusing the active key id in the historical keyring fails closed. Missing, weak, or ambiguous key configuration never mints or verifies an authority receipt; the original receipt remains preserved as historical evidence while the claim package stays exploratory. Secret values and secret-derived fingerprints are never exported; ops/governance surfaces report only strength, opaque key id, collision readiness, and historical-key count.

Session lifecycle management is also exposed in the `/workspace/sena` Enterprise runtime Account security panel. Signed-in users can list active sessions, identify the current session, revoke individual non-current sessions, or revoke all other sessions from the workbench; API clients can use `GET /api/auth/sessions` and `DELETE /api/auth/sessions` with a CSRF token for the same lifecycle controls.

Password reset is available through `/api/auth/password-reset` and `/reset-password`. `POST {"action":"request","email":"..."}` queues a reset without revealing whether the account exists. `POST {"action":"confirm","resetToken":"...","password":"..."}` applies `sena-enterprise-password-policy/v1`, updates the password, revokes existing sessions, clears failed-login lockouts, and records audit evidence. For local smoke tests only, set `SENA_PASSWORD_RESET_EXPOSE_TOKEN=1` to return a local reset token and URL in the API response; production-like deployments should keep that unset and connect an institution-approved email or notification provider.

Notifications are available through `/api/sena/notifications`. The local enterprise runtime queues an in-app outbox record for team invitations, password-reset requests, project comments, reliability reviews, and validation reviews without storing raw recipient email addresses. Authenticated users can query their visible notifications and mark them as read; team managers can query team-scoped delivery evidence and `POST` the same route to run the webhook delivery/retry worker. Email and notification webhook workers persist a time-bounded delivery claim before invoking the provider, use the stable delivery/notification ID as an idempotency key, and atomically record the attempt after the provider returns; an unrelated state write therefore cannot orphan a sent message or replace the provider result with a whole-state CAS error. When `SENA_ENTERPRISE_STATE_STORE=postgres` is active, the notifications route reads and updates the primary Postgres state row for list, read status, notification delivery status, sealed email-delivery queue status, and audit evidence instead of mutating `.sena-enterprise/enterprise-db.json`. Set `SENA_NOTIFICATION_WEBHOOK_URL`, optional `SENA_NOTIFICATION_WEBHOOK_SECRET`, `SENA_NOTIFICATION_WEBHOOK_MAX_ATTEMPTS`, and `SENA_NOTIFICATION_WEBHOOK_TIMEOUT_MS` to send redacted signed notification payloads to an institution-approved event bus, email bridge, or workflow service. Production-like deployments should still document the final email provider, retention policy, and monitoring owner.

Collaboration pub/sub delivery is available through `/api/sena/projects/:projectId/collaboration`. The local SSE stream remains available for single-runtime browser updates, while presence, comment, comment-resolution, and adjudication actions also enqueue signed external delivery events when `SENA_COLLABORATION_PUBSUB_WEBHOOK_URL` is configured. When `SENA_ENTERPRISE_STATE_STORE=postgres` is active, collaboration route mutations use the Postgres primary-state row first and then mirror comments, active presence, adjudications, and refreshed reliability coverage to indexed Postgres tables for collaboration reads and ops evidence. Team managers can call `POST {"action":"deliver-pubsub","force":true}` on the collaboration route to send `sena-enterprise-collaboration-pubsub-webhook/v1` payloads with `x-sena-webhook-event: collaboration.publish`, endpoint-hash evidence, retry status, and optional HMAC signing from `SENA_COLLABORATION_PUBSUB_WEBHOOK_SECRET`. Use `SENA_COLLABORATION_PUBSUB_WEBHOOK_MAX_ATTEMPTS` and `SENA_COLLABORATION_PUBSUB_WEBHOOK_TIMEOUT_MS` to tune replay behavior; team backups exclude `collaborationEvents` as transient delivery queue state.

Organization provisioning is available through `/api/sena/provisioning`. Set `SENA_PROVISIONING_TOKEN` and call the endpoint with `Authorization: Bearer ...` to upsert teams, users, SSO identities, and memberships from an institution IdP. In production, also set non-secret `SENA_PROVISIONING_TOKEN_SECRET_REF` secret-store custody evidence, `SENA_PROVISIONING_TOKEN_VERSION` rotation evidence, and `SENA_IDENTITY_LIFECYCLE_OWNER_MODE` to `scim`, `idp`, or `hybrid` so the platform decision record explicitly binds lifecycle ownership before SCIM/IdP ownership evidence can pass. Provisioned records store source, external ID, and last-sync metadata; audit logs store counts and hashes rather than raw credentials. The runtime enforces the same last-active-team-manager guardrail used by manual membership changes.

SCIM 2.0 provisioning is available through `/api/sena/scim/v2/ServiceProviderConfig`, `/api/sena/scim/v2/Users`, and `/api/sena/scim/v2/Groups` using the same bearer token. The bridge accepts standard SCIM User/Group payloads, the standard Enterprise User organization extension, and the SENA extension schemas `urn:sena:params:scim:schemas:extension:enterprise:2.0:User` and `urn:sena:params:scim:schemas:extension:enterprise:2.0:Group` for role-aware team memberships, SSO subject mapping, and group member roles. SCIM `active:false` maps to suspended memberships while preserving the last-active-team-manager guardrail.

Ops monitoring is available through `/api/sena/ops/status`, `/api/sena/ops/metrics`, `/api/sena/ops/readiness`, `/api/sena/ops/cdn`, `/api/sena/ops/postgres`, `/api/sena/ops/object-storage`, `/api/sena/ops/jobs/worker-contract`, `/api/sena/ops/observability`, `/api/sena/ops/observability/probe`, `/api/sena/ops/production-evidence`, `/api/sena/ops/deployment`, `/api/sena/ops/native-adapters`, `/api/sena/ops/saas-operations`, `/api/sena/ops/go-live-rehearsal`, `/api/sena/ops/platform-decisions`, `/api/sena/ops/release-gate`, and `/api/sena/ops/alerts`. Set `SENA_OPS_TOKEN` for bearer-token access from deployment monitors; without it, the monitor endpoints require an authenticated SENA session. Platform decision and release-gate reviews always require a session plus team-management RBAC. The JSON status reports runtime uptime, storage readability/writability, write-lock health, write-before backup existence, upload blob integrity, collaboration pub/sub webhook configuration and queue health, database-sync webhook configuration, object-storage webhook configuration, backup freshness, backup webhook configuration, alert webhook configuration, queue counters, and collection counts. The metrics endpoint emits Prometheus-style gauges such as readiness, storage writability, backup age, collaboration pub/sub/database-sync/object-storage/backup/alert webhook configuration, Postgres/object-storage/job-queue/observability live probe readiness, production evidence manifest completeness, collection counts, queue counts, ops-token configuration, request p95, request error rate, route sample counts, worker-contract readiness, and observability exporter readiness. The production evidence endpoint emits `sena-enterprise-production-evidence-manifest/v1`, aggregating redacted artifact custody for the Vercel production preflight, Postgres, object storage, CDN, server job queue, external worker contract, observability, the production performance budget, and the conference load rehearsal. The observability endpoint emits `sena-enterprise-observability-sli/v1` with current-process request SLI samples, route summaries, p95/error-rate SLO status, slow-request samples, and redacted exporter/dashboard evidence; `/api/sena/ops/observability/probe` emits `sena-enterprise-observability-probe/v1` by sending a signed synthetic SLI sample to the configured exporter. Request IDs, paths, query strings, payload values, URL values, and secrets are excluded or hashed. The readiness endpoint returns a production-gate artifact with blocking checks for storage, lock health, write-before backup, ops token, backup freshness, signed backup webhook delivery, signed alert webhook delivery, signed database-sync delivery, signed upload object-storage delivery, signed collaboration pub/sub delivery, OIDC, provisioning, notification webhook signing, institution email webhook signing, audit, and RBAC, plus advisory platform-decision checks for production `NODE_ENV`, managed database/object storage adapter choice, upload missing/corrupt/orphan evidence, secret hardening, validation evidence, deployment package evidence, and monitoring ownership. The deployment endpoint emits `sena-enterprise-organization-deployment/v1`, a redacted handoff package with required env inventory, service endpoints, readiness/governance summaries, OIDC provider status, platform-decision ownership, `sena-enterprise-platform-decision-register/v1`, latest release-gate review and verification evidence, and verification commands; the native-adapters, SaaS-operations, and go-live-rehearsal endpoints emit standalone `sena-enterprise-native-adapter-certification/v1`, `sena-enterprise-saas-operations-readiness/v1`, and `sena-enterprise-go-live-rehearsal/v1` dossiers for platform-owner review before cutover. The register tracks full SaaS backend operations until the institution platform owner approves the bridge model or replaces it with native managed database, object storage, pub/sub, email, alerting, audit, backup, and IdP adapters. `POST /api/sena/ops/platform-decisions` records `sena-enterprise-platform-decision-acceptance/v1` evidence for accepted, rejected, native-adapter-required, or superseded decisions; accepted bridge records are counted in the deployment package, native adapter certification, governance evidence, and SaaS operations readiness. Set `SENA_PLATFORM_SAAS_OPERATING_MODEL_APPROVED=1` only after that platform-owner approval is recorded. Secret values are excluded, endpoint values are hashed, and secret hashing is disabled to avoid creating reusable secret fingerprints. The alerts endpoint emits `sena-enterprise-ops-alerts/v1` with firing critical/warning items derived from ops status, readiness, and request-level SLI breaches, plus alerting ownership evidence from `SENA_ALERTING_OWNER`, optional `SENA_ALERTING_CHANNEL`, and optional `SENA_ALERTING_RUNBOOK_URL`; `POST {"action":"deliver"}` sends a signed `sena-enterprise-ops-alert-webhook/v1` payload to `SENA_ALERT_WEBHOOK_URL` with endpoint-hash evidence and optional HMAC signing from `SENA_ALERT_WEBHOOK_SECRET`. Set `SENA_ALERT_WEBHOOK_TIMEOUT_MS` to tune delivery timeout. Set `SENA_ENTERPRISE_DB_LOCK_TIMEOUT_MS` to tune single-runtime file lock timeout; multi-instance SaaS deployments should use an accepted bridge record with release-gate evidence or replace the local file adapter with a managed database or lock-capable durable storage.

Production performance readiness is exposed inside `/api/sena/ops/readiness` as `sena-enterprise-production-performance-path/v1` and in `/api/sena/ops/metrics` as `sena_enterprise_production_performance_*` gauges. It keeps SENA's current local file runtime scoped to research-pilot use and blocks production readiness until the deployment path has explicit evidence for native Postgres state configuration, managed upload object storage, CDN gzip/brotli plus immutable static asset caching, a managed server job queue plus external worker contract for heavy analysis/export work, request-level observability with alert ownership, a current `npm run sena:performance:check` artifact-budget pass, and a 50-user 30-minute conference load rehearsal artifact. The performance check now emits a redacted `sena-enterprise-production-performance-budget/v2` JSON artifact plus `performanceBudgetArtifactSha256`. From a clean Git worktree, create a fresh production build, run the check with `SENA_PERFORMANCE_BUDGET_BINDABLE_REQUIRED=1`, archive the artifact, and use `npm run sena:production-evidence:bind`; do not hand-assemble a partial performance env tuple. The artifact carries hashes for the Next build ID and `package-lock.json` plus the Git commit and dirty/clean status; the binder accepts performance-budget evidence only from a clean Git worktree and independently remeasures the exact local `.next` artifact set before binding. Set `SENA_ENTERPRISE_DB_ADAPTER=postgres` or `neon`, a Postgres URL, and `SENA_ENTERPRISE_STATE_STORE=postgres` before claiming database readiness; that switch also moves upload registry metadata into the indexed `sena_enterprise_uploads` Postgres table, mirrors import run metadata and cleaning-manifest lineage into the indexed `sena_enterprise_import_runs` Postgres table, mirrors analysis run metadata and artifact fingerprints into the indexed `sena_enterprise_analysis_runs` Postgres table, mirrors coding-reliability run metadata and adjudication coverage into the indexed `sena_enterprise_reliability_runs` Postgres table, mirrors validation run metadata, preregistration hashes, parity evidence, formal inference status, and review status into the indexed `sena_enterprise_validation_runs` Postgres table, mirrors expert-review metadata, target bindings, claim scope, ratings, and review timestamps into the indexed `sena_enterprise_expert_reviews` Postgres table, moves server job receipts and worker status updates from `enterprise-db.serverJobs` into the indexed `sena_enterprise_server_jobs` Postgres table, and moves async audit writes plus audit JSON/CSV/integrity reads into the indexed `sena_enterprise_audit_log` Postgres table. Run `npm run sena:postgres:verify` or call `/api/sena/ops/postgres` to create the redacted `sena-enterprise-postgres-probe/v1` artifact that proves the managed database can create the dedicated probe table, insert, read, and delete without exposing connection strings, probe IDs, or secret values. Set `SENA_ENTERPRISE_POSTGRES_LIVE_PROBE_REQUIRED=1`, `SENA_ENTERPRISE_POSTGRES_LIVE_PROBE_CONFIRMED=1`, `SENA_ENTERPRISE_POSTGRES_PROBE_ARTIFACT_SHA256`, and `SENA_ENTERPRISE_POSTGRES_PROBE_VERIFIED_AT` when the deployment gate must reject Postgres configuration without a live probe artifact. Set `SENA_OBJECT_STORAGE_ADAPTER=s3|r2|gcs-s3`, `SENA_OBJECT_STORAGE_ENDPOINT`, `SENA_OBJECT_STORAGE_BUCKET`, `SENA_OBJECT_STORAGE_ACCESS_KEY_ID`, and `SENA_OBJECT_STORAGE_SECRET_ACCESS_KEY`, or use the signed bridge with `SENA_OBJECT_STORAGE_WEBHOOK_URL` and `SENA_OBJECT_STORAGE_WEBHOOK_SECRET`; for native buckets, run `npm run sena:object-storage:verify` or call `/api/sena/ops/object-storage` to create the redacted `sena-enterprise-object-storage-probe/v1` artifact that proves PUT, HEAD, and DELETE cleanup without exposing endpoint, bucket, object-key, or secret values. Set `SENA_OBJECT_STORAGE_LIVE_PROBE_REQUIRED=1`, `SENA_OBJECT_STORAGE_LIVE_PROBE_CONFIRMED=1`, `SENA_OBJECT_STORAGE_PROBE_ARTIFACT_SHA256`, and `SENA_OBJECT_STORAGE_PROBE_VERIFIED_AT` when the deployment gate must reject native object-storage configuration without a live probe artifact. For local pilot checks, `SENA_CDN_ENABLED=1`, `SENA_CDN_COMPRESSION_CONFIRMED=1`, and optional `SENA_CDN_STATIC_ASSET_CACHE_SECONDS` can document manual CDN/compression setup. For production runtime, `SENA_REQUIRE_PRODUCTION_PERFORMANCE_PATH=1`, `SENA_PRODUCTION_EVIDENCE_MANIFEST_REQUIRED=1`, or `SENA_PLATFORM_SAAS_OPERATING_MODEL_APPROVED=1`, run `npm run sena:cdn:verify` against `SENA_CDN_VERIFY_URL` and archive the redacted `sena-enterprise-cdn-probe/v1` artifact, then set `SENA_CDN_LIVE_PROBE_CONFIRMED=1`, `SENA_CDN_PROBE_ARTIFACT_SHA256`, and `SENA_CDN_PROBE_VERIFIED_AT`; the production gate rejects manual compression claims without that live probe artifact. Set `SENA_JOB_QUEUE_ADAPTER=managed` or `webhook` with `SENA_JOB_QUEUE_URL`, `SENA_JOB_QUEUE_SECRET`, optional `SENA_JOB_QUEUE_TIMEOUT_MS`, optional `SENA_JOB_QUEUE_MAX_ATTEMPTS`, and optional `SENA_JOB_QUEUE_TIMESTAMP_SKEW_SECONDS` (default 300, ceiling 3600) which bounds how long a signed worker webhook stays replayable — the `x-sena-webhook-timestamp` header is folded into the HMAC and its freshness is enforced symmetrically, so a captured request expires rather than replaying indefinitely. `/api/sena/analyze` accepts `queue:true` or `Prefer: respond-async`, dispatches a signed `sena-enterprise-server-job-queue-webhook/v2` payload to `SENA_JOB_QUEUE_URL`, persists a redacted `sena-enterprise-server-job/v2` receipt, and returns `202`; its production heavy-job gates require that async path and reject missing queue configuration. `/api/sena/exports/publication` requires a persisted `projectId`, rejects inline snapshots before publication-domain side effects, and applies the same model-card, approved-current-reliability, validation, expert-review, adjudication, and state-revision gate to queued and synchronous project exports. Queued requests persist an encrypted command envelope and return `202 sena-enterprise-server-job/v2`; the executable worker reopens the envelope, revalidates the exact evidence lease, stores a content-addressed artifact receipt, and fails closed on drift. A 202 queue receipt is not publication success. Queue dispatch failures return `503` instead of pretending the job was accepted. `/api/sena/ops/jobs` lists `sena-enterprise-server-job-list/v1` records and accepts worker status actions `mark-running`, `mark-succeeded`, `mark-failed`, `retry`, and `dead-letter`, storing only job metadata and redacted error hashes for retry/DLQ monitoring. `/api/sena/ops/jobs/worker-contract` returns `sena-enterprise-server-job-worker-contract/v1`; set `SENA_JOB_WORKER_RUNTIME`, `SENA_JOB_WORKER_CALLBACK_URL`, `SENA_JOB_WORKER_RUNBOOK_URL`, `SENA_JOB_WORKER_OWNER` or `SENA_ALERTING_OWNER`, `SENA_JOB_WORKER_HEARTBEAT_CONFIRMED=1`, `SENA_JOB_WORKER_HEARTBEAT_SHA256`, and `SENA_JOB_WORKER_HEARTBEAT_VERIFIED_AT`, then run `npm run sena:jobs:worker-contract` and archive the contract artifact hash before claiming worker readiness. Run `npm run sena:jobs:queue-verify` or call `/api/sena/ops/jobs/probe` to create the redacted `sena-enterprise-server-job-queue-probe/v1` artifact proving the managed queue accepts a signed synthetic no-user-data payload; set `SENA_JOB_QUEUE_LIVE_PROBE_REQUIRED=1`, `SENA_JOB_QUEUE_LIVE_PROBE_CONFIRMED=1`, `SENA_JOB_QUEUE_PROBE_ARTIFACT_SHA256`, and `SENA_JOB_QUEUE_PROBE_VERIFIED_AT` when production readiness must reject unproven queue dispatch. `/api/sena/ops/cdn` runs the same live CDN probe for deployment monitors without exposing URL values. `/api/sena/ops/observability` and the Prometheus metrics route expose request p95, error rate, route sample counts, and slow-request evidence; set `SENA_OBSERVABILITY_PROVIDER`, `SENA_OBSERVABILITY_EXPORTER_URL`, `SENA_OBSERVABILITY_EXPORTER_SECRET`, `SENA_OBSERVABILITY_DASHBOARD_URL`, `SENA_OBSERVABILITY_RUNBOOK_URL`, optional `SENA_OBSERVABILITY_SLO_P95_MS`, optional `SENA_OBSERVABILITY_SLO_ERROR_RATE_PERCENT`, and `SENA_OBSERVABILITY_OWNER` or `SENA_ALERTING_OWNER` before claiming production observability. Queued analysis and validation jobs require persisted project pointers, while import and reliability jobs use registered upload pointers. `SENA_JOB_QUEUE_ALLOW_INLINE_PAYLOAD` is retained only as deprecated configuration evidence and is ignored by current admission; raw inline sources never enter queue receipts or dispatch payloads. Set `SENA_OPS_TOKEN`, `SENA_ALERT_WEBHOOK_URL`, `SENA_ALERT_WEBHOOK_SECRET`, and `SENA_ALERTING_OWNER`; then set `SENA_PERFORMANCE_BUDGET_CONFIRMED=1` only after the current build passes the performance budget check. The `.sena-enterprise/enterprise-db.json` store remains a local pilot artifact, not the production multi-user state backend.

The current `npm run sena:jobs:worker-heartbeat` artifact is a same-process status-store CAS self-test, not an external worker heartbeat. It uses an exact internal `synthetic-heartbeat-v1` no-user-data profile only to write the active job store and call the status-transition functions in the same process. It does not observe managed-queue delivery, an external worker process, or an authenticated callback receipt, so it cannot satisfy production worker readiness even when all legacy heartbeat environment variables are configured. Production remains under review until a nonce-bound managed-queue -> external-worker -> authenticated-callback receipt is implemented and independently bindable. Normal analysis enqueue cannot select the synthetic profile, and local polling excludes it while separately quarantining delivered queued analysis receipts with missing or partial custody markers before claim without incrementing attempts; the existing attempt count is preserved, without reclassifying them as legacy or making them executable.

The production performance path also includes `production-runtime-header`. After Vercel production is redeployed with Postgres primary state, run `npm run sena:vercel:preflight`, archive the passed `sena-enterprise-vercel-production-preflight/v1` artifact, and bind `SENA_VERCEL_PRODUCTION_PREFLIGHT_CONFIRMED`, `SENA_VERCEL_PRODUCTION_PREFLIGHT_ARTIFACT_SHA256`, and `SENA_VERCEL_PRODUCTION_PREFLIGHT_VERIFIED_AT`. The production gate keeps this item under review while `www.sena.hk` still reports `x-sena-runtime: enterprise-local`.

In strict production modes (`NODE_ENV=production`, `SENA_REQUIRE_PRODUCTION_PERFORMANCE_PATH=1`, `SENA_PRODUCTION_EVIDENCE_MANIFEST_REQUIRED=1`, `SENA_PLATFORM_SAAS_OPERATING_MODEL_APPROVED=1`, or `SENA_PERFORMANCE_BUDGET_BINDABLE_REQUIRED=1`), `npm run sena:performance:check` fails unless the emitted performance-budget artifact has a clean, bindable build identity: the canonical Next BUILD_ID, Git commit, clean status, deployable-source hash, `package-lock.json` hash, and stable measured-output hash. Production binding and archiving independently remeasure the exact local `.next` output set and all three Brotli values; a rewritten JSON artifact plus an ordinary `.sha256` sidecar is insufficient while the controlled build output remains unchanged. This local mechanism assumes the release build root itself is trusted; it is not a signed CI attestation against an actor who can rewrite `.next` and regenerate every local checksum. That stronger threat model requires an immutable CI artifact digest or signed external build provenance. `npm run sena:performance:source-custody` remains available only as a redacted diagnostic inventory for research handoff; it cannot authorize or bind a dirty build. Commit the intended release slice, confirm the worktree is clean, create a fresh production build, and then rerun the strict performance check. Advisory mode is for local sizing diagnostics, not release evidence.

QStash is supported as a managed server-job queue mode through `SENA_JOB_QUEUE_ADAPTER=qstash`. Set `SENA_JOB_WORKER_CALLBACK_URL` or `SENA_JOB_QUEUE_URL` as the worker destination, keep `SENA_JOB_QUEUE_SECRET` for SENA's own HMAC worker contract, and provide `SENA_JOB_QUEUE_PROVIDER_TOKEN` or `QSTASH_TOKEN` for the QStash API. Optional `SENA_JOB_QUEUE_PROVIDER_URL` or `QSTASH_URL` defaults to `https://qstash.upstash.io`; optional `SENA_JOB_QUEUE_NAME` or `QSTASH_QUEUE_NAME` uses QStash enqueue semantics instead of direct publish. SENA forwards its worker headers through `Upstash-Forward-*`, so the external worker still verifies the same signed payload.

Vercel Blob is supported as a native object-storage mode through `SENA_OBJECT_STORAGE_ADAPTER=vercel-blob`. Use `SENA_OBJECT_STORAGE_BLOB_READ_WRITE_TOKEN` or `BLOB_READ_WRITE_TOKEN` for static read-write-token auth, or `SENA_OBJECT_STORAGE_BLOB_STORE_ID` / `BLOB_STORE_ID` with Vercel OIDC when the Blob store is connected to the project. SENA maps upload custody and live probes onto Blob SDK `put`, `head`, and `del` calls and stores only redacted endpoint/store/object-key hash evidence.

All production verifier scripts accept `--output <file>` to write the redacted JSON artifact plus `<file>.sha256` even when the verifier returns nonzero, including Postgres, object storage, CDN, server job queue, worker contract, observability, performance budget, conference load, and the production evidence manifest. Use this for release-evidence custody rather than relying on terminal scrollback.

After a live probe, performance budget, or conference rehearsal artifact passes, use `npm run sena:production-evidence:bind -- --artifact output/production-evidence/<artifact>.json --scope <vercel-team-slug>` to dry-run the Vercel production evidence env updates, then rerun with `--yes` to write only the matching evidence tuple to the Vercel `production` environment. For a clean-worktree performance artifact the binder writes one complete 10-key tuple: `SENA_PERFORMANCE_BUDGET_CONFIRMED`, `SENA_PERFORMANCE_BUDGET_ARTIFACT_SHA256`, `SENA_PERFORMANCE_BUDGET_VERIFIED_AT`, `SENA_PERFORMANCE_BUDGET_SCHEMA_VERSION`, `SENA_PERFORMANCE_BUDGET_MEASURED_ARTIFACT_SET_SHA256`, `SENA_PERFORMANCE_BUDGET_NEXT_BUILD_ID_SHA256`, `SENA_PERFORMANCE_BUDGET_GIT_COMMIT`, `SENA_PERFORMANCE_BUDGET_GIT_DIRTY`, `SENA_PERFORMANCE_BUDGET_PACKAGE_LOCK_SHA256`, and `SENA_PERFORMANCE_BUDGET_SOURCE_CUSTODY_MODE`. Legacy reviewed-slice manifest/tree hashes are not part of the bindable clean-worktree tuple. Do not configure these fields one at a time: at runtime they are a trusted deployment assertion handed off by the binder, not an independently signed proof. The production evidence manifest checks the tuple's current schema, formats, measured-output hash, and asserted clean status; the stronger threat model still requires immutable or signed external CI provenance as described above. `--evidence-dir output/production-evidence/<release-id>` scans a whole release evidence directory. A ready `sena-enterprise-production-evidence-archive/v1` can also be bound directly with `--artifact output/production-evidence/<release-id>/sena-production-evidence-archive.json`; the binder expands its child artifact files only when the archive status is `ready` and the archive `.sha256` custody file matches. The binder skips failed/review artifacts, requires each child `.sha256` custody file to match the artifact JSON, and never reads or prints service endpoint values, bucket names, database URLs, or secrets.

Use `npm run sena:production-evidence:archive -- --output-dir output/production-evidence/<release-id> --vercel-scope <vercel-team-slug>` to run the Vercel preflight plus production verifier set into one schema-versioned `sena-enterprise-production-evidence-archive/v1` bundle. The archive also emits `production-runtime-env-packet.json` as an advisory child artifact so the same release folder carries the current provider-readiness handoff packet; this packet can be `blocked` and bindable, but it never replaces the live Postgres, object-storage, CDN, job-queue, observability, performance, or load-rehearsal evidence. The archive intentionally skips the 50-user, 30-minute rehearsal unless `--include-load` is provided, and that skipped rehearsal remains a production blocker rather than being treated as evidence. Use `--skip-vercel-preflight` only for offline diagnostics; it also remains a production blocker because deployment shell evidence is required before handoff.

When production `NODE_ENV` is combined with `SENA_REQUIRE_PRODUCTION_PERFORMANCE_PATH=1`, or when `SENA_PRODUCTION_EVIDENCE_MANIFEST_REQUIRED=1` or `SENA_PLATFORM_SAAS_OPERATING_MODEL_APPROVED=1` is set, direct file-backed enterprise state writes are blocked with `503 enterprise_file_state_production_write_blocked`, even if Postgres primary state is configured. Production code should use the primary-state APIs that route writes to Postgres; the legacy direct file helpers remain research-pilot compatibility surfaces only. If the local JSON file is absent, read-only ops reporting returns a non-persisted empty state instead of initializing `.sena-enterprise/enterprise-db.json`, so production-claim gates cannot accidentally create or mutate the research-pilot file store.

With `SENA_ENTERPRISE_STATE_STORE=postgres`, `/api/sena/reliability` creates, lists, adjudicates, and reviews coding-reliability runs through the primary `sena_enterprise_state` row; reliability run metadata is mirrored to `sena_enterprise_reliability_runs`, and generated adjudication payloads are mirrored to `sena_enterprise_adjudications` for indexed ops evidence.

`/api/sena/reliability` fails closed at a shared admission boundary before reliability parsing, reviewer-envelope persistence, queue dispatch, audit, or run persistence. A request may use exactly one source mode: JSON `files`/`annotations`/`rows`/`data`, multipart `files`, or registered `uploadIds`; every supplied JSON alias counts toward admission even when legacy semantic precedence selects one alias. When the JSON `files` property is supplied it must be an array; an explicit empty array remains a supported canonical empty dataset and is preserved across synchronous and queued execution. Limits are 200,000 supplied raw rows, 100 sources, 25 MiB per reliability source, and 100 MiB aggregate source bytes. On the server, the effective per-source limit is the lower of 25 MiB and `SENA_UPLOAD_MAX_BYTES`. The complete statistical-work budget is 10,000,000 evaluated pairwise/cell operations and the retained pair-result budget is 100,000 entries, both with safe-integer overflow rejection before project lookup or writes. The POST HTTP transport envelope is capped at 128 MiB and 8,192 yielded body chunks: `Content-Length` provides an early fast-fail; JSON is structurally scanned in at most 64 KiB decoded slices; and multipart framing, part/source fan-out, per-source bytes, and aggregate source bytes are scanned before calling `request.json()` or `request.formData()`. PATCH review/adjudication control envelopes use a separate 65,536-byte (64 KiB) cap and the same 8,192-chunk cap before authentication or state access; absent, understated, and oversized `Content-Length` values cannot bypass the streamed-byte check. Optional review and adjudication `notes` are string-only and capped at 8,192 UTF-8 bytes before project/state reads, dashboard recomputation, audit, notification, persistence, or Postgres mirroring. A maximum 100-record adjudication batch therefore copies at most about 819,200 note bytes. CSV rows/cells and XLSX ZIP/worksheet rows are likewise bounded before their materializing decoders run. An absent or understated declaration, a single large stream chunk, malformed input, duplicate/escaped JSON aliases, sparse XLSX row indices, and a zero-byte chunk storm therefore cannot bypass admission. Zero-byte chunks consume the read-count budget but are not retained for replay. Adjudication mutations resolve at most 100 previously unresolved disagreements per request; an omitted limit that would exceed the cap or an explicit limit above 100 returns stable `413 reliability_adjudication_limit_exceeded`, while zero, fractional, or unsafe limits return `400 reliability_adjudication_limit_invalid` before writes. Repeated bounded batches advance through only the still-unresolved queue. Admission rejections use stable sanitized `400` errors and never echo row values.

Queued JSON reliability sources are lossless relative to synchronous execution for supported `files` arrays and `annotations`/`rows`/`data` aliases. Every queue provider encrypts one already-admitted source envelope per logical JSON file or alias plus the reviewer envelope, then dispatches only opaque registered-upload pointers and hashes. The worker independently re-admits those pointer-backed raw envelopes before claiming the job, then reuses the admitted result so source summaries, invalid-row warnings, empty-value skipped cells, annotation coverage, and agreement estimates do not drift between synchronous and queued runs. A failed reliability pre-admission leaves the queued job lifecycle unchanged, and the local file-backed claim holds its lock across the queued-state predicate and transition so independent worker processes cannot both claim the same job.

For production Postgres readiness, adapter and state-store configuration are necessary but not enough. Production runtime, `SENA_REQUIRE_PRODUCTION_PERFORMANCE_PATH=1`, `SENA_PRODUCTION_EVIDENCE_MANIFEST_REQUIRED=1`, or `SENA_PLATFORM_SAAS_OPERATING_MODEL_APPROVED=1` require a current `npm run sena:postgres:verify` artifact proving CREATE TABLE, INSERT, SELECT, and DELETE cleanup, bound through `SENA_ENTERPRISE_POSTGRES_LIVE_PROBE_CONFIRMED=1`, `SENA_ENTERPRISE_POSTGRES_PROBE_ARTIFACT_SHA256`, and `SENA_ENTERPRISE_POSTGRES_PROBE_VERIFIED_AT`.

For the `www.sena.hk` Vercel deployment, use `npm run sena:vercel:neon:configure -- --yes --scope <vercel-team-slug>` after a pooled Neon/Postgres connection string is available in `SENA_ENTERPRISE_POSTGRES_URL`, `SENA_DATABASE_URL`, `DATABASE_URL`, `POSTGRES_URL`, `POSTGRES_PRISMA_URL`, or `NEON_DATABASE_URL` in the shell or local env file; use `--local-env-file <path>` if the local file is not `.env.local`. To avoid putting the database URL in shell history, prefer `read -rs SENA_NEON_URL` followed by `printf '%s\n' "$SENA_NEON_URL" | npm run sena:vercel:neon:configure -- --yes --postgres-url-stdin --scope <vercel-team-slug>`. The script writes Vercel production env vars for `SENA_ENTERPRISE_DB_ADAPTER=neon`, `SENA_ENTERPRISE_STATE_STORE=postgres`, `SENA_ENTERPRISE_POSTGRES_URL`, `SENA_APP_URL=https://www.sena.hk`, and `NEXT_PUBLIC_SENA_APP_URL=https://www.sena.hk`, ensures the domain is attached to the linked Vercel project, and never prints the database URL. Use `--remove-legacy-file-env` during the Postgres cutover to remove `SENA_ENTERPRISE_DB_DIR` from Vercel production after the Postgres env write; `--strict-production` automatically enables that removal plan. The Vercel preflight and runtime header resolver accept the same pooled URL env-name set when `SENA_ENTERPRISE_DB_ADAPTER` and `SENA_ENTERPRISE_STATE_STORE` are present; direct/non-pooling URLs should be copied into `SENA_ENTERPRISE_POSTGRES_URL` only after the platform owner has accepted that connection model. Add `--deploy` only when the current tree is ready to send to Vercel production; add `--strict-production` only after the remaining object-storage, CDN, server-job-queue, observability, performance-budget, and conference-load evidence is ready to become a hard gate.

Use `npm run sena:vercel:production-services:configure -- --services all --scope <vercel-team-slug>` to dry-run the remaining Vercel production service env contract for object storage, server job queue, and observability/alerting. The script reads values from process env, `--local-env-file <path>`, or `--env-json-stdin`, stops before any Vercel write if a required variable is missing, and never prints env values, endpoint URLs, bucket names, or secrets. For Cloudflare R2, it can normalize `R2_ACCOUNT_ID`, `R2_BUCKET_NAME`, `R2_ACCESS_KEY_ID`, and `R2_SECRET_ACCESS_KEY` into canonical `SENA_OBJECT_STORAGE_*` values, deriving `https://<account>.r2.cloudflarestorage.com` and `SENA_OBJECT_STORAGE_REGION=auto` for the existing S3-compatible PUT/HEAD/DELETE probe. For Vercel Blob, it can normalize `BLOB_READ_WRITE_TOKEN` or `BLOB_STORE_ID` into `SENA_OBJECT_STORAGE_ADAPTER=vercel-blob` plus canonical Blob env names. For Upstash QStash, it can normalize `QSTASH_TOKEN`, optional `QSTASH_URL`, optional `QSTASH_QUEUE_NAME`, `SENA_JOB_WORKER_CALLBACK_URL`, and `SENA_JOB_QUEUE_SECRET` into the canonical SENA queue env contract. Re-run with `--yes` only after the real provider values are available; after any real provider write, follow the printed verification plan to regenerate and bind `production-runtime-env-packet.json` from the fresh preflight/archive evidence before relying on its provider-readiness summary. Add `--strict-production` only when missing live-probe artifacts should become hard production blockers. Example secure stdin usage:

Before entering real provider values, generate the redacted production runtime env packet with `npm run sena:production-env:packet -- --domain https://www.sena.hk --scope <vercel-team-slug> --preflight output/production-evidence/vercel-production-preflight-current.json --archive output/production-evidence/current-advisory/sena-production-evidence-archive.json --output output/production-evidence/production-runtime-env-packet.json`. The emitted `sena-enterprise-production-runtime-env-packet/v1` artifact summarizes the current Neon/Postgres, object storage, CDN, server job queue, observability, clean-build performance, and 50-user rehearsal blockers; includes placeholder-only JSON templates for Vercel Blob or Cloudflare R2 plus QStash and observability; and excludes domain, Vercel scope, endpoint, bucket, database URL, and secret values. A `blocked` packet is expected until the real provider values, live probes, clean performance artifact, and conference rehearsal evidence are attached. Use `npm run sena:production-evidence:bind -- --artifact output/production-evidence/production-runtime-env-packet.json --scope <vercel-team-slug>` to dry-run binding its advisory custody hash into Vercel production env; this records the handoff packet but does not replace Postgres, object-storage, queue, observability, performance, or load-rehearsal probe evidence.

```bash
printf '%s\n' '{"SENA_OBJECT_STORAGE_ADAPTER":"vercel-blob","BLOB_READ_WRITE_TOKEN":"...","SENA_JOB_QUEUE_ADAPTER":"qstash","QSTASH_TOKEN":"...","QSTASH_QUEUE_NAME":"...","SENA_JOB_WORKER_CALLBACK_URL":"https://www.sena.hk/api/sena/ops/jobs","SENA_JOB_QUEUE_SECRET":"...","SENA_ALERTING_OWNER":"...","SENA_ALERT_WEBHOOK_URL":"...","SENA_ALERT_WEBHOOK_SECRET":"...","SENA_OBSERVABILITY_PROVIDER":"...","SENA_OBSERVABILITY_EXPORTER_URL":"...","SENA_OBSERVABILITY_EXPORTER_SECRET":"...","SENA_OBSERVABILITY_DASHBOARD_URL":"...","SENA_OBSERVABILITY_RUNBOOK_URL":"..."}' \
  | npm run sena:vercel:production-services:configure -- --env-json-stdin --yes --scope <vercel-team-slug>
```

Run `npm run sena:vercel:preflight -- --scope <vercel-team-slug> --output output/production-evidence/vercel-production-preflight.json` to archive a redacted `sena-enterprise-vercel-production-preflight/v1` artifact before and after configuring Neon. The preflight checks the linked Vercel production deployment, `www.sena.hk` domain binding, HTTPS response headers, the `x-sena-runtime` production header, and production env variable names only; it excludes env values and hashes deployment URL evidence. `x-sena-runtime` must report `enterprise-neon` or `enterprise-postgres` after the Postgres primary state switch is deployed; `enterprise-local` remains a research-pilot runtime signal and keeps the Vercel preflight in review. `SENA_ENTERPRISE_DB_DIR` is treated as a legacy local-file state signal in Vercel production, so the preflight keeps `legacy-local-file-env` under review until Postgres primary state is configured and that variable is removed from production env. Bind a passed artifact through `SENA_VERCEL_PRODUCTION_PREFLIGHT_CONFIRMED=1`, `SENA_VERCEL_PRODUCTION_PREFLIGHT_ARTIFACT_SHA256`, and `SENA_VERCEL_PRODUCTION_PREFLIGHT_VERIFIED_AT`, or let `npm run sena:production-evidence:bind` set them from the matching artifact/archive. Passing this preflight does not replace the live Postgres/object-storage/CDN/job-queue/observability probes.

For production server-job readiness, managed/webhook/QStash queue configuration is necessary but not enough. Production runtime, `SENA_REQUIRE_PRODUCTION_PERFORMANCE_PATH=1`, `SENA_PRODUCTION_EVIDENCE_MANIFEST_REQUIRED=1`, or `SENA_PLATFORM_SAAS_OPERATING_MODEL_APPROVED=1` require a current `npm run sena:jobs:queue-verify` artifact bound through `SENA_JOB_QUEUE_LIVE_PROBE_CONFIRMED=1`, `SENA_JOB_QUEUE_PROBE_ARTIFACT_SHA256`, and `SENA_JOB_QUEUE_PROBE_VERIFIED_AT`, plus a passed `npm run sena:jobs:worker-contract` artifact bound through `SENA_JOB_WORKER_CONTRACT_CONFIRMED=1`, `SENA_JOB_WORKER_CONTRACT_ARTIFACT_SHA256`, and `SENA_JOB_WORKER_CONTRACT_VERIFIED_AT`.

For production object-storage readiness, native bucket configuration and upload custody are necessary but not enough. Production runtime, `SENA_REQUIRE_PRODUCTION_PERFORMANCE_PATH=1`, `SENA_PRODUCTION_EVIDENCE_MANIFEST_REQUIRED=1`, or `SENA_PLATFORM_SAAS_OPERATING_MODEL_APPROVED=1` require a current `npm run sena:object-storage:verify` artifact proving PUT, HEAD, and DELETE cleanup, bound through `SENA_OBJECT_STORAGE_LIVE_PROBE_CONFIRMED=1`, `SENA_OBJECT_STORAGE_PROBE_ARTIFACT_SHA256`, and `SENA_OBJECT_STORAGE_PROBE_VERIFIED_AT`.

For production observability readiness, exporter, dashboard, runbook, owner, alerting, audit-table, and Postgres observed-request sample-store configuration are necessary but not enough. Production runtime, `SENA_REQUIRE_PRODUCTION_PERFORMANCE_PATH=1`, `SENA_PRODUCTION_EVIDENCE_MANIFEST_REQUIRED=1`, or `SENA_PLATFORM_SAAS_OPERATING_MODEL_APPROVED=1` require a current `npm run sena:observability:verify` artifact proving signed exporter delivery, bound through `SENA_OBSERVABILITY_LIVE_PROBE_CONFIRMED=1`, `SENA_OBSERVABILITY_PROBE_ARTIFACT_SHA256`, and `SENA_OBSERVABILITY_PROBE_VERIFIED_AT`.

Request-level observability sample storage has two explicit modes. The local research-pilot path uses a current-process ring buffer, while `SENA_ENTERPRISE_STATE_STORE=postgres` plus the configured Postgres adapter mirrors redacted observed requests to the indexed `sena_enterprise_observed_requests` table; `/api/sena/ops/observability` reads that durable window when active, and `/api/sena/ops/metrics` exposes `sena_enterprise_observability_sample_store_postgres`.

Password registration/login, session verification, MFA, password reset, API SSO, team invitations, membership lifecycle APIs, import/analyze/upload project persistence, validation run persistence, claim-package evidence, notification delivery status, ops status counts, governance health exports, and project-scoped publication export now follow the active primary enterprise state store on the API path. With `SENA_ENTERPRISE_STATE_STORE=postgres`, `/api/auth/register`, `/api/auth/login`, `/api/auth/me`, `/api/auth/logout`, `/api/auth/sessions`, `/api/auth/mfa`, `/api/auth/password-reset`, `/api/auth/sso`, `/api/auth/sso/callback`, `/api/sena/team`, `/api/sena/team/invitations`, `/api/sena/team/memberships`, `/api/sena/import`, `/api/sena/analyze`, `/api/sena/uploads`, `/api/sena/validation/group-comparison`, `/api/sena/validation/claim-package`, `/api/sena/notifications`, `/api/sena/ops/status`, `/api/sena/governance/health`, `/api/sena/exports/publication`, CSRF-failure audit capture, and public auth rate-limit buckets read/write or report from the primary `sena_enterprise_state` row instead of storing or deriving those records from `.sena-enterprise/enterprise-db.json`. Password-reset request/confirm also keeps email/notification queue metadata, session revocation, lockout clearing, and audit evidence in the same primary row. MFA login challenge failure audit no longer writes directly to the file-backed store when the login API is using primary state. SSO preflight audit, OAuth state start/callback, local fallback user/session creation, and fallback invitation acceptance use the same primary row. Invitation create/accept/revoke, team-state reads, membership role/status changes, notification list/read/delivery state, sealed email-delivery queue status, ops status notification/email queue counters, and governance health counts use the same primary row. Import/analyze project creation, project updates, run-history reads, upload registry state, import-run state, analysis-run state, validation-run state, validation review notifications, project-scoped claim-package reads, and publication export audit use the same primary row while selected upload/import/analysis/validation registry metadata is mirrored to indexed Postgres tables for ops evidence. The synchronous local helpers remain available for research-pilot fixtures and scripts; broad SaaS claims still require live managed Postgres, object storage, CDN, server job queue, observability, backup, 30-minute conference-load evidence, and institution-owned identity operations evidence.

Run `npm run sena:production-evidence:check -- --output output/production-evidence/sena-enterprise-production-evidence-manifest.json` or call `/api/sena/ops/production-evidence` after all live-probe, performance-budget, and conference-load artifact hashes plus verified-at timestamps are configured. The CLI writes the redacted manifest plus `.sha256` custody file before returning nonzero on missing required evidence, so blocked handoffs can still archive their exact evidence gap. Set `SENA_PRODUCTION_EVIDENCE_MANIFEST_REQUIRED=1` when missing production evidence should block the handoff with 503.

The primary-state API coverage includes `/api/sena/reliability` alongside auth, team, import, analyze, upload, validation, claim-package, and publication export paths. Reliability-run state, adjudication state, review notifications, and reliability audit entries use the active primary row when Postgres primary state is configured.

`/api/sena/validation/expert-review` also follows the active primary enterprise state store for create, list, review, notification, and audit state; the indexed `sena_enterprise_expert_reviews` table remains the ops-evidence mirror rather than the only source of truth.

Conference-scale performance rehearsal is available through `npm run sena:conference:load-check`. By default it can be used as a short smoke against `SENA_LOAD_TARGET_URL` or `SENA_APP_URL`, but production evidence for the 50-person meeting scenario must be generated with `SENA_LOAD_REQUIRE_PRODUCTION_TARGET=1`, `SENA_LOAD_TARGET_USERS=50`, and `SENA_LOAD_DURATION_SECONDS=1800` against the deployed URL. Set `SENA_LOAD_RAMP_SECONDS=120` when using the generated production runbook so the rehearsal records a two-minute linear ramp followed by a 30-minute sustained measurement window; the artifact separately reports ramp requests, sustained requests, sustained p95, and sustained error rate while the release gate keeps the overall p95/error-rate checks conservative. Archive the emitted `sena-enterprise-conference-load-rehearsal/v1` JSON plus `conferenceLoadRehearsalArtifactSha256`, then set `SENA_CONFERENCE_LOAD_REHEARSAL_CONFIRMED=1`, `SENA_CONFERENCE_LOAD_REHEARSAL_ARTIFACT_SHA256`, `SENA_CONFERENCE_LOAD_REHEARSAL_VERIFIED_AT`, `SENA_CONFERENCE_LOAD_REHEARSAL_USERS`, `SENA_CONFERENCE_LOAD_REHEARSAL_DURATION_SECONDS`, `SENA_CONFERENCE_LOAD_REHEARSAL_P95_MS`, and `SENA_CONFERENCE_LOAD_REHEARSAL_ERROR_RATE_PERCENT`. The production evidence manifest and performance path treat that artifact as separate from the build-size budget so a short local smoke cannot be mistaken for the 30-minute conference rehearsal; in production runtime, both the build-size budget artifact and the conference rehearsal artifact are blocking release evidence, not advisory notes.

Before running the 50-user rehearsal, generate the redacted rehearsal runbook with `npm run sena:conference:prepare -- --target-url https://www.sena.hk --scope <vercel-team-slug> --preflight output/production-evidence/vercel-production-preflight.json --archive output/production-evidence/<release-id>/sena-production-evidence-archive.json --output output/production-evidence/conference-rehearsal-plan.json`. The emitted `sena-enterprise-conference-rehearsal-plan/v1` artifact separates hard blockers from evidence gaps, keeps target URL and team scope values out of the JSON, and prints command templates for the ramped one-minute smoke, the ramped 50-user/30-minute sustained rehearsal, Vercel evidence binding, and the final production evidence archive. It also enforces the production evidence freshness window from `SENA_PRODUCTION_EVIDENCE_MAX_AGE_HOURS`, defaulting to 168 hours, so stale preflight or archive artifacts cannot authorize the full rehearsal. A `blocked` plan means Postgres primary state, runtime header, service probes, performance-budget evidence, or evidence freshness is not ready for the full rehearsal; `ready-for-rehearsal` means the full load can be run next; `ready-for-conference` means the full load and archive evidence are already attached and fresh.

The go-live rehearsal response also includes `sena-enterprise-release-gate-draft/v1`, a release-gate draft that can prefill the workspace review form but keeps human approval and real verifier evidence separate from the generated rehearsal evidence. It also embeds `sena-enterprise-go-live-rollback-drill/v1`; `GET /api/sena/ops/go-live-rehearsal?artifact=rollback-drill` exports the standalone backup, restore-dry-run, incident-alerting, release-gate, and verifier runbook evidence for cutover review. `GET /api/sena/ops/go-live-rehearsal?artifact=post-cutover-monitor` exports `sena-enterprise-go-live-monitor/v1`, a 60-minute observation-window dossier with ops status, firing alert counts, rollback readiness, release verification, exit criteria, next actions, and latest `sena-enterprise-post-cutover-observations/v1` evidence.
Team managers can `POST` the same route to persist `sena-enterprise-go-live-attestation/v1` cutover attestations; approved attestations are rejected until the current rehearsal and persisted post-cutover monitor are ready, while blocked/conditional attestations preserve the rehearsal blockers, release-gate draft status, rollback drill evidence, post-cutover monitor evidence, checklist evidence, and audit trail. The same session plus CSRF protected route accepts `{"action":"start-post-cutover-observation","teamId":"...","environment":"...","releaseVersion":"..."}`, `{"action":"record-post-cutover-sample","teamId":"...","observationId":"..."}`, and `{"action":"complete-post-cutover-observation","teamId":"...","observationId":"...","acknowledgedWarningAlertIds":[]}` to create the fixed 60-minute `sena-enterprise-post-cutover-observation/v1` record required for approved go-live.
The enterprise capability audit is available through `/api/sena/ops/capability-audit` as `sena-enterprise-capability-audit/v1`, mapping the original missing-feature backlog to runnable endpoint, UI contract, artifact, and remaining platform-decision evidence.

Heavy import, analysis, reliability, and validation work can be routed through the server job queue. `/api/sena/import`, `/api/sena/analyze`, `/api/sena/reliability`, and `/api/sena/validation/group-comparison` accept `queue:true` or `Prefer: respond-async`; the production async-heavy-job env gates require that path. Import and multipart reliability jobs register uploads and send upload IDs rather than raw file bytes. Project-scoped analysis and validation jobs send project pointers. Queued inline analysis/validation snapshots or datasets are rejected with `server_job_inline_source_custody_required`, even if the legacy inline-payload flag is set; their synchronous routes remain supported. For every queue provider, JSON reliability sources, the reviewer envelope, and the complete admitted project-scoped analysis command are encrypted as registered uploads before dispatch. The public analysis receipt retains only the command envelope's opaque upload id and SHA-256. Both managed/webhook execution and local polling decrypt that immutable command, validate its upload profile, receipt hash, embedded payload hash, final canonical payload hash, immutable source title, and retained project revision, and complete those checks before the job can be claimed. Receipt-only external workers cannot bypass this gate through the generic `mark-running` callback: current analysis receipts are re-admitted server-side, while only explicitly marked `legacy-inline-v2` receipts retain the narrow envelope-free compatibility path.

Queued source-backed work uses a durable two-step claimability fence. The job receipt is first written with `delivery.sourceReady=false`, so recovery and failure evidence can see it while polling workers, file-store claims, Postgres compare-and-set claims, and `mark-running` callbacks all reject it. Only after project-pointer custody, the encrypted analysis-command envelope, import uploads, or reliability source/reviewer-envelope artifacts are durable does the enqueue path persist `sourceReady=true`; dispatch occurs after that transition. Validation project pointers are claimable only when their retained `projectTeamId` is a JSON string exactly equal to the job team. Analysis execution reads the immutable retained `projectVersion`, cross-checks the signed `expectedVersion` against the redacted receipt and retained revision, replays title, description, build options, human review, coding reliability, and governance from the encrypted command, and compare-and-swaps any requested project update against that admitted version. An immutable command whose project revision, encrypted envelope, or exact payload can no longer be reproduced is atomically terminalized before claim without incrementing attempts; the existing attempt count is preserved instead of leaving the command queued indefinitely. A source-persistence failure retains a non-claimable failed receipt, and `retry` returns `409 server_job_source_repair_required` without changing that receipt; the source must be resubmitted. Legacy receipts that lack `sourceReady` use the same non-persisting read projection in file and Postgres stores: unambiguous delivered/local-sink or queue-dispatch evidence projects true, while ambiguous pending and source-persistence failure project false. Worker lifecycle transitions form an explicit state machine and bind terminal callbacks to the persisted `workerRunId`; Postgres applies those checks in a single compare-and-set update.

Job-record `retry` is a local polling-queue operation only: a source-ready local failure may move back to `queued` within its attempt budget, and concurrent retries use the same single-winner state transition. Managed, webhook, and QStash jobs are never marked retryable by the SENA receipt store; they require provider-native delivery retry or a fresh source-bound submission, and an operator `retry` request returns `409 server_job_resubmission_required`. The v1 machine contracts retain `inlinePayloadRequiresExplicitEnv` only as a `null` compatibility tombstone alongside `inlinePayloadAllowed=false`; no environment value enables inline queue custody.

Current-v2 validation source replay also applies a request-wide pre-hash boundary. Project, revision, analysis-run, and nested fingerprint authority carriers remain exact: accessors, Symbol/non-enumerable/extra keys, unsupported prototypes, and ill-formed UTF-16 keys or values are rejected before authority fields are read or hashed. Untrusted dataset and build-option sources instead use a bounded schema projection: only declared own data fields and declared dense array indices are read, while arbitrary extra object or array keys are neither enumerated nor traversed; known-field accessors, sparse or oversized arrays, unsupported prototypes, and malformed declared values still fail closed. The projection and structural walker share the request's remaining byte/work allowance and stop at the first exhausted reservation; every declared source string is counted in UTF-8 bytes against the 16 MiB source limit. Source identity hashes only the canonical projected source with streaming SHA-256 rather than a materialized canonical JSON string, and every distinct cloned carrier charges cumulative digest bytes and work even when its digest reuses an existing model. Historical validation indexes still exact-admit every revision wrapper and identity, but only deep-admit and hash snapshots matching the required project/team/version; normalize and seal share that scoped index and hash cache.

Publication export remains project-bound and fail-closed. The enterprise route requires a persisted `projectId`: a missing or blank project returns `400 publication_export_project_required`; an additional inline `snapshot` returns `400 publication_export_inline_snapshot_forbidden` before publication-domain side effects. Synchronous and async claim-ready exports use the same model-card, approved-current-reliability, sealed-validation, receipt-authenticated expert-review, adjudication, and atomic state-binding checks. Async requests persist an encrypted command envelope and return `202 sena-enterprise-server-job/v2`; the worker reopens the envelope, re-reads the authoritative state, verifies the project version, snapshot hash and stable authorization digest, generates the artifact, stores a SHA-256 result receipt, and fails closed on drift. EvidenceFlow exploratory runs use a separate package-only `sena-workflow-exploratory-publication/v1` path: it explicitly states `claimBoundary=exploratory-only`, includes gate status and limitations, excludes raw datasets/upload values/checkpoints/credentials, and never upgrades the strict publication route or authorizes inference.

### SENA EvidenceFlow

EvidenceFlow is the SENA-owned control plane for two fixed durable graphs: `research-evidence/v1` and `engineering-release/v1`. SENA Postgres records are authoritative for runs, transactional outbox commands, hash-chained step receipts, approvals, artifact references, and closeout. LangGraph checkpoints live separately in the `sena_langgraph` schema and provide execution recovery only; they are not research or release authority. Each workflow run is also its LangGraph `thread_id`. Research starts must explicitly classify their source as `fixture` or `approved-pseudonymized`; the classification is stored and digest-bound, governance preflight verifies passed upload custody plus complete governance and opaque/no-roster metadata, and fixture runs can never become `inference-ready`. The v1 worker has no LangSmith dependency and must run as the long-lived Node service `npm run sena:workflows:worker-run`, never inside a Vercel request lifecycle. Configure the same authoritative Postgres connection used by the enterprise runtime, set `SENA_WORKFLOW_WORKER_ID` in production, and optionally tune `SENA_WORKFLOW_WORKER_POLL_MS`, `SENA_WORKFLOW_WORKER_MAX_ATTEMPTS`, and `SENA_WORKFLOW_WORKER_LEASE_MS`. `SENA_WORKFLOW_WORKER_ONCE=1` performs one bounded local probe.

The API surface is `/api/sena/workflows/definitions`, `/api/sena/workflows/runs`, `/api/sena/workflows/runs/{runId}`, `/api/sena/workflows/runs/{runId}/events`, `/api/sena/workflows/runs/{runId}/actions`, and `/api/sena/workflows/runs/{runId}/closeout`. Cookie-auth mutations require CSRF, `expectedVersion`, `Idempotency-Key`, team ownership, and node-specific RBAC. A `202` response means only that a command is durable. `409` reports version/idempotency/source drift, `422` reports invalid evidence or policy input, and `503` reports a genuinely unavailable queue or executable capability. The events stream is redacted and never returns checkpoint state, raw research rows, private logs, direct identifiers, or credentials.

Research starts bind an immutable project revision plus registered import and reliability uploads by ID, SHA-256, size, import profile, and scan status. Import, fusion analysis, reliability, validation, and publication reuse the existing server-job queue and executable workers. Workflow completion and scientific readiness remain separate: a run may succeed with `claimBoundary=exploratory-only`; only complete governance, reliability/adjudication, sealed validation, and expert approval can produce `inference-ready`. Exploratory publication is package-only and keeps its limitations visible.

Engineering v1 is shadow-only. Admission binds the work request, protected-main repository preflight, A01-A15 owner lane, branch/worktree hash, exact base and candidate SHA, changed-path digest, allowlist, and independent focused/typecheck/lint/build/pilot receipts. Real SENA targets remain read-only and leave CI, merged, deployed, and live evidence as `not-run`; only an isolated fixture repository may provide the complete PR-head, post-merge-main, deployment, rollback, and live-proof simulation receipts. No v1 configuration authorizes a Git push, PR mutation, merge, provider change, or deployment.

Institution email delivery for password resets and team invitations is available through the same notifications endpoint. Set `SENA_EMAIL_WEBHOOK_URL`, optional `SENA_EMAIL_WEBHOOK_SECRET`, `SENA_EMAIL_WEBHOOK_MAX_ATTEMPTS`, and `SENA_EMAIL_WEBHOOK_TIMEOUT_MS`, then call `POST /api/sena/notifications` with `{"action":"deliver-email","teamId":"..."}` to send signed `sena-enterprise-email-webhook/v1` payloads to the approved email bridge. Email delivery payloads include the real recipient address and secure reset/invite action URL only inside the signed email webhook body; the local queue stores them sealed with AES-256-GCM, ordinary in-app notifications keep only email hashes/domains, and team backups exclude `emailDeliveries`.

Audit governance is available through `/api/sena/governance/audit`. Team managers can query JSON, export CSV, or add `?integrity=1` to include `sena-enterprise-audit-integrity/v1`, a scoped SHA-256 linked audit-chain head plus retention evidence. When `SENA_ENTERPRISE_STATE_STORE=postgres` is active, async audit writes and audit JSON/CSV/integrity queries use the indexed `sena_enterprise_audit_log` table; otherwise `enterprise-db.auditLog` remains the research-pilot fallback. Set `SENA_AUDIT_RETENTION_DAYS` to the institution-approved retention window before production audit retention is claimed. Set `SENA_AUDIT_WEBHOOK_URL`, optional `SENA_AUDIT_WEBHOOK_SECRET`, `SENA_AUDIT_WEBHOOK_MAX_ATTEMPTS`, and `SENA_AUDIT_WEBHOOK_TIMEOUT_MS`, then `POST` the audit endpoint to forward signed `sena-enterprise-audit-webhook/v1` payloads with chain-head evidence to institutional logging or SIEM storage. Forwarded payloads hash email, token, password, invite-code, and secret-like detail fields before delivery.

Upload object-storage delivery is available through `/api/sena/uploads`. Team managers can `POST {"action":"deliver-object-storage","teamId":"...","uploadId":"..."}` to verify the local blob, confirm SHA-256 integrity, and deliver it to object storage; omit `uploadId` to deliver a team batch with an optional `limit`. When `SENA_OBJECT_STORAGE_ADAPTER=s3|r2|gcs-s3` plus endpoint, bucket, region, access key, and secret are configured, SENA performs a native S3-compatible signed `PUT`; when `SENA_OBJECT_STORAGE_ADAPTER=vercel-blob` plus Blob token or OIDC store credentials are configured, SENA performs native Vercel Blob `put`/`head`/`del` operations. Native delivery returns only redacted endpoint/store hash evidence, ETag hash evidence, and object-key hashes. Without the native adapter it falls back to a signed `sena-enterprise-upload-object-storage-webhook/v1` payload to `SENA_OBJECT_STORAGE_WEBHOOK_URL`. Each upload records redacted `objectStorageCustody` metadata after delivery attempts, including delivered/failed/skipped status, provider mode, object-key hash, endpoint/bucket hashes, version or ETag hash, timestamps, and redacted error hashes; when `SENA_ENTERPRISE_STATE_STORE=postgres` is active, upload registry metadata and custody updates are mirrored to the indexed `sena_enterprise_uploads` table. `/api/sena/ops/object-storage` and `npm run sena:object-storage:verify` run a separate native bucket live probe for PUT, HEAD, and DELETE cleanup evidence without exposing endpoint, bucket/store, object-key, token, or secret values. Files with local scan status `review` are skipped unless `includeReview:true` is provided, and failed local integrity checks audit as `upload.object_storage.fail`. The production performance path keeps object storage in review while any passed upload is still pending delivery, any delivery has failed, or any review-held upload remains unresolved. Set `SENA_OBJECT_STORAGE_TIMEOUT_MS` for native delivery timeout or `SENA_OBJECT_STORAGE_WEBHOOK_TIMEOUT_MS` for bridge timeout.

Local upload blobs are stored as AES-256-GCM envelopes (`sena-upload-aes-256-gcm-envelope/v1`) under `.sena-enterprise/uploads/...`; `SENA_UPLOAD_ENCRYPTION_KEY` should be supplied for beta or production-like environments, while the pilot fallback key is derived from the local DB path and is not portable custody evidence. The `.sena-enterprise/` directory is gitignored and remains a local research-pilot store only. Beta deployments should pair `SENA_ENTERPRISE_STATE_STORE=postgres` with the configured Postgres adapter so upload metadata, custody state, and audit evidence are mirrored to managed persistence, then use native object-storage delivery plus the live PUT/HEAD/DELETE probe as the retention/deletion record for uploaded blobs. Team-scoped backups intentionally exclude upload blob bytes; local blob retention is bounded by the local workspace lifecycle, and deleting `.sena-enterprise/` removes the pilot auth/team/upload store after any required managed-object-storage custody and audit exports have been archived.

Managed database sync delivery is available through `/api/sena/governance/backup`. Team managers can `POST {"action":"sync-database","teamId":"..."}` to create a team-scoped sanitized enterprise backup, verify checksum/counts/secret exclusions, and send a signed `sena-enterprise-database-sync-webhook/v1` payload to `SENA_DATABASE_SYNC_WEBHOOK_URL`; `POST {"action":"sync-database","artifact":...}` sends an already exported artifact after the same permission and preflight checks. Payloads use the backup artifact as the canonical sanitized state snapshot, include record counts and payload SHA-256, and never include sessions, SSO state, lockouts, API rate-limit buckets, MFA secrets, email delivery queues, reset tokens, presence, collaboration pub/sub queue events, password hashes, or upload blobs. Set `SENA_DATABASE_SYNC_WEBHOOK_URL`, optional `SENA_DATABASE_SYNC_WEBHOOK_SECRET`, and optional `SENA_DATABASE_SYNC_WEBHOOK_TIMEOUT_MS` to connect the local enterprise runtime to an institution-approved managed database adapter.

Backup restore rehearsal is available through `/api/sena/governance/backup`. `GET` exports a team-scoped backup, `POST` without an action verifies checksum/counts/secret exclusions, `POST {"action":"deliver","teamId":"..."}` creates, verifies, and sends a signed `sena-enterprise-backup-webhook/v1` payload to `SENA_BACKUP_WEBHOOK_URL`, `POST {"action":"deliver","artifact":...}` sends an already exported artifact after permission and core verification checks, `POST {"action":"restore-dry-run","artifact":...}` reports the merge plan without writing restored records, and `POST {"action":"restore","artifact":...}` merges the scoped payload by ID after checksum, record-count, and secret-exclusion checks pass. Set `SENA_BACKUP_WEBHOOK_URL`, optional `SENA_BACKUP_WEBHOOK_SECRET`, and optional `SENA_BACKUP_WEBHOOK_TIMEOUT_MS` to connect scheduled backups to institution-managed storage or a database bridge. Restore never reintroduces sessions, SSO state, lockouts, API rate-limit buckets, MFA secrets, email delivery queues, reset tokens, presence, collaboration pub/sub queue events, password hashes, or upload blobs.

## Demo Flow

1. Open `/workspace/sena`.
2. Use the sticky six-step workflow navigator to move between Data Import, Model Builder, Fusion Canvas, Evidence, Temporal Trace, and Report; each step shows `Ready` or `Review` from the current pilot audits. Switch the central plot from the top Plots bar directly under the header: seven always-visible view tabs (Fusion, Dual Lens, Temporal, ENA Space, SNA, Evidence, Matrix), the active Window and Turns context chip, and the collapsed `Plots` menu with per-view descriptions; the central deck title follows the active view. The dark header stays focused on dataset scale, report readiness, upload, and export, and the Plot Tools rail panel tunes layers, thresholds, and temporal framing while pointing back to the Plots bar for view switching.
3. Click `Load lesson-study sample` in the Data Import panel, or download `Lesson-study JSON` and upload it manually.
4. After signing in, use the Enterprise runtime Team operations panel to create team invitations, accept invitation codes, revoke pending invites, and update member roles or active/suspended status from the workbench.
5. Check `Data contract audit` in Data Import, then export `sena-data-contract-audit.json` if you need a standalone five-table contract record.
6. Adjust `alpha`, `beta`, `gamma`, normalization, and edge threshold in Model Builder, then confirm the archived formula audit remains available through runtime bundle or review packet exports.
7. Switch between Explanatory, ENA Space, and Joint layouts. ENA Space uses jENA projected unit points and code node positions when the manifest is computed.
8. Read the A1 Inner Solid Mesh layer key in Fusion Canvas, then select nodes or edges and inspect evidence. The central workspace defaults to a current-window Fusion Plot and shows a S/W/B/G evidence capsule with the strongest social tie, ENA concept tie, bridge tie, and person-code-pair signal for that active temporal window. The central active-window brief turns those signals into a conservative interpretation scaffold with evidence cues, full-conversation ranks, review checks, and guardrails before researchers write claims. The right Secondary Plot adds a current-window comparison lens that contrasts the active window with the full conversation for SNA density, S ties, ENA W links, B bridges, G total, and `A_fusion` total before evidence inspection; it also ranks the active-window top S/W/B/G signals against the full-conversation baseline so reviewers can see whether a local signal is globally dominant or window-specific. Day-mode readability uses light link halos and on-demand high-contrast label plates: node labels are hidden by default, and clicking a person or concept node reveals only that selected node label. S/W/B line thickness is layer-relative and salience-scaled, so stronger SNA outer arcs, ENA inner mesh links, and bridge ribbons render with heavier strokes; when active-window `W` values are tied, concept-pair `G` contribution provides a transparent ENA tie-breaker. Rendered links retain `data-edge-weight`, `data-edge-normalized-weight`, `data-edge-scaled-weight`, `data-edge-visual-salience`, and `data-edge-visual-width` provenance. Edge selections show line-weight provenance plus matrix provenance, including visual salience, stroke width, runtime source, matrix block, formula factor, fusion slot, and guardrail. Concept-edge selections also show `G attribution`, so a traditional ENA `W` link can be read with its person-code-pair contributors. The key distinguishes SNA outer-orbit blue arcs, solid internal ENA W mesh links, B bridge ribbons, and G pair contributions while showing the active weights and threshold.
9. Review Evidence Ledger and export `sena-evidence-ledger.json` as a human-review queue. Each evidence snippet includes five-table lineage such as source table, row id, utterance, segment, person, code, or window identifiers.
10. Review the Stats rail metric provenance summary. The Stats rail panel adds a compact `sena-metric-provenance/v1` summary that separates direct jSNA, jENA, SENA-derived, SENA-implemented, and composite metrics before interpretation; the exported metric provenance JSON also carries social, epistemic, and fusion snapshots so reviewers can trace each source category back to S/W/B/G/A_fusion evidence. Export `sena-jena-manifest.json`, `sena-ena-report.json`, `sena-jsna-manifest.json`, `sena-sna-report.json`, `sena-metric-provenance.json`, `sena-person-code-pair-g-report.json`, `sena-development-plan.json`, `sena-runtime-consistency-audit.json`, or `sena-runtime-bundle.json` when you need runtime, jENA report, development-scope, SNA, metric-provenance, G-contribution, or runtime-consistency artifacts. Method protocol, visual grammar, and fusion math audit records remain archived in the review packet and related bundle/report outputs.
11. Change Temporal Trace mode between Stage, Moving, and Turn, then review the Temporal Fusion Arc and `Temporal Runtime Trace` for per-window jENA/jSNA status, S/W/B metrics, G pair contributions, A_fusion checksum, adjacent-window transition evidence, and the top G pair driving each window.
12. Inspect Method Validation for metric provenance, alpha/beta/gamma sensitivity, normalization sensitivity, community stability, temporal coverage, and lightweight null-model checks. The metric provenance table distinguishes direct jSNA, jENA, SENA-derived, SENA-implemented, and SENA composite metrics, with parity status and interpretation limits.
13. Fill the Report Generator human-review, coding-reliability, and data-governance fields, record manual status/reviewer/notes in the on-page demo verification checklist, and check the pilot-readiness, claim-readiness, development-plan, verification-compatibility, review-packet, and completeness audits before exporting.
14. Export project snapshot JSON, runtime bundle JSON, runtime consistency audit JSON, jENA manifest JSON, jENA report JSON, jSNA manifest JSON, SNA report JSON, metric provenance JSON, G contribution report JSON, data-contract audit JSON, development plan JSON, production page contract JSON, demo walkthrough JSON, demo verification JSON, demo verification compatibility audit JSON, temporal runtime trace JSON, evidence ledger JSON, pilot-readiness JSON, coding-reliability gate JSON, claim-readiness gate JSON, review packet JSON, report JSON, report Markdown, or enterprise publication exports as SVG, PNG, XLSX, DOCX, PDF, and a manifest-backed `sena-publication-package/v1` JSON package with per-artifact SHA-256 evidence, `sena-publication-source-snapshot/v1` source-snapshot evidence, `sena-data-governance-metadata/v1` ethics/consent/retention metadata, and `sena-publication-verification-certificate/v1` artifact/gate checks. Save or open a persisted server project before requesting an enterprise publication export. Publication exports are blocked with `publication_export_model_card_blocked` until `modelCard.renderGate.status` is ready, so uncarded figures and packages are not generated. Report and review-packet exports include the active analysis window, current-window vs full-conversation comparison metrics, current-window S/W/B/G signal ranking context, active-window interpretation brief, data-contract, pilot-readiness, claim-readiness, coding-reliability, formula, method protocol, visual grammar, verification-compatibility, and completeness audits for parameters, S/W/B/G matrices, jENA, jSNA, metric implementation provenance, per-window temporal S/W/B/G narrative with A_fusion checksums, top G pair labels, evidence, validation, guardrails, warnings, data-governance metadata, human-review fields, and demo verification manual-review records where applicable.

Every synchronous publication artifact independently carries the same `sena-publication-derivation-manifest/v3`: HTML uses an application/JSON script, SVG uses metadata, PNG uses an UTF-8 iTXt chunk, XLSX uses a `Derivation Manifest` worksheet, DOCX carries a visible digest plus embedded JSON, PDF attaches the JSON manifest, and the package carries it both at the top level and inside every bundled artifact. Enterprise publication requires a persisted project, so the manifest distinguishes the raw persisted snapshot, compatibility read-projection, and rendered publication snapshot hashes and includes the atomic state binding, claim-package hash, approved reliability-run hash, and adjudication-coverage hash. Standalone library exports set persisted/read-projection boundaries to `null` rather than relabeling an inline snapshot as stored evidence. The response header `x-sena-publication-derivation-manifest-sha256` identifies the embedded manifest in every format.
15. Re-upload `sena-project-snapshot.json` or `sena-review-packet.json` to restore the local dataset, model weights, normalization, temporal mode, active window, human-review fields, coding-reliability fields, data-governance fields, and demo verification manual-review records. The review packet path restores from its embedded `contents.projectSnapshot`, so a reviewer handoff packet can now reopen editable workspace state directly. Re-uploading `sena-demo-verification.json` reapplies only the checklist manual-review records when its dataset counts and build options match the current workspace; if the compatibility gate reports a mismatch, load the matching snapshot/review packet or recreate the matching dataset/options before applying those records.

## Pilot Data Assets

The public demo assets live in:

```text
public/sena-pilot/
```

The Data Import panel separates downloadable sample-dataset files from blank template files, and both lists are driven by the shared `lib/sena/pilot-assets.ts` manifest. The bundled lesson-study sample includes Hong Kong classroom-style bilingual evidence excerpts, so both the one-click JSON contract and five-table CSV upload path exercise non-English evidence preservation in reports, Markdown, and project snapshots. The pilot package manifest also carries `assetIntegrity` fingerprints (`bytes` and `sha256`) for every bundled sample and template file so reviewers can verify that a local handoff is using the expected assets.

Package manifest:

- `public/sena-pilot/sena-pilot-package-manifest.json`

Blank templates:

- `public/sena-pilot/templates/people.csv`
- `public/sena-pilot/templates/interactions.csv`
- `public/sena-pilot/templates/utterances.csv`
- `public/sena-pilot/templates/coded_segments.csv`
- `public/sena-pilot/templates/codebook.csv`
- `public/sena-pilot/templates/sena-data-contract-template.json`

Sample lesson-study dataset:

- `public/sena-pilot/sample/lesson-study-sena-contract.json`
- `public/sena-pilot/sample/lesson-study-people.csv`
- `public/sena-pilot/sample/lesson-study-interactions.csv`
- `public/sena-pilot/sample/lesson-study-utterances.csv`
- `public/sena-pilot/sample/lesson-study-coded_segments.csv`
- `public/sena-pilot/sample/lesson-study-codebook.csv`

The CSV headers match aliases in `lib/sena/import.ts`, so the sample package should import without manual field remapping.

Exported project snapshots use `schemaVersion: "sena-project-snapshot/v1"`. A snapshot is the local pilot's restorable model JSON: it contains the full source dataset, the active analysis window, the window-scoped analysis dataset, build options, runtime provenance, graph nodes and edges, S/W/B/G/fusion matrices, temporal runtime trace with per-window matrix fingerprints, report output, human-review fields, coding-reliability fields, and demo verification manual-review records. Uploading a snapshot or review packet back into `/workspace/sena` sends the parsed source to the public, same-origin `/api/sena/snapshot/restore` validation boundary before any workspace state setter runs. Before `JSON.parse`, that handler bounds streamed bytes, yielded chunks, nesting depth, structural tokens, containers, total members, and per-container fan-out. Structural allowance is the lower of the configured-byte-ceiling maximum (4,194,304 tokens at the default 16 MiB limit) and a padding-invariant density allowance `max(250,000, floor(nonStringCarrierUnits * 3 / 4))`; bytes inside strings and JSON whitespace cannot purchase parser fan-out. Default caps are 262,144 containers, 2,097,152 total members, 65,536 members in one container, and depth 64. Container/member caps scale only with the validated `SENA_SNAPSHOT_RESTORE_MAX_BYTES` ceiling (up to 2× at its 32 MiB configuration maximum), not with actual request bytes or padding; depth remains fixed. The exact resolved limits are propagated through direct project snapshots, project handoffs, and embedded review-packet snapshots, while the canonical analysis and review-packet catalog budgets remain fixed. These caps preserve an independently probed current-builder 146-code canonical snapshot at 15,822,919 bytes (2,388,412 structural tokens, 177,684 containers, 1,426,319 total members, maximum container fan-out 10,589, depth 12) while rejecting padded dense-object requests before parsing. Review-packet catalog fields add explicit cardinality limits (256 artifact or handoff entries, 256 assets per group, 512 integrity/evidence/guardrail entries as applicable) and use precomputed sets for cross-membership, so their validation work is bounded and linear rather than pairwise. Canonical validation evidence is capped at 1,000 typed issues. The handler then applies the same project-handoff/review-packet validators used by server restore paths and returns a `sena-snapshot-restore-result/v1` read projection with independently recomputable source/projection SHA-256 values. It writes no project, revision, enterprise audit event, or application raw-payload log; the response explicitly reports `persisted: false` and `audited: false`. Explicit cross-origin browser requests are rejected, but the snapshot still crosses the deployment's same-origin server and therefore remains subject to HTTPS, ingress, retention, and infrastructure logging policy; upload only data authorized for that deployment. The pilot-readiness audit includes a `model-json-export` gate so reviewer handoff checks that this model JSON can carry graph structure, S/W/B/G, fusion, and temporal trace evidence.

Exported demo verification checklists use `schemaVersion: "sena-demo-verification/v1"`. Uploading a checklist back into `/workspace/sena` reapplies the manual-review records (`pending`, `passed`, `failed`, reviewer, verified timestamp, and notes) to the current model without changing the dataset, weights, temporal settings, or report text. The sample-import checklist item carries `assetIntegrity=12`, `assetIntegritySha256=12`, and `handoff=pilot-asset-integrity` evidence so reviewers can verify the bundled sample/template files as part of the manual walkthrough. The import is gated by `sena-demo-verification-compatibility/v1`, which compares dataset counts and build options before applying records; mismatched checklists are rejected until the matching snapshot, dataset, and options are active again. Runtime bundles and review packets include `sena-demo-verification-compatibility-audit.json` content so this gate is visible during reviewer handoff.

Exported method protocols use `schemaVersion: "sena-method-protocol/v1"`. A method protocol records the S/W/B/G layer definitions, `A_fusion` block equation, selected weights and temporal settings, local jENA/jSNA runtime provenance including API surfaces, companion artifacts including the pilot package manifest, standalone metric-provenance artifact, standalone coding-reliability gate, and standalone claim-readiness gate, adopted Fusion Canvas and Temporal Fusion Arc visual grammar, and interpretation guardrails. The SENA Formula workspace panel is archived for now; formula audit and matrix fingerprint records remain available through report, runtime bundle, and review-packet outputs.

Exported runtime bundles use `schemaVersion: "sena-runtime-bundle/v1"`. The bundle includes `artifactEvidence`, a structured index of the main jENA, jSNA, metric-provenance, G, runtime-audit, pilot package manifest, coding-reliability gate, and SENA bundle downloads with each artifact's schema, UI export control, source runtime, matrix coverage, evidence coverage, and handoff checks such as `jena-concept-matrix`, `jsna-social-matrix`, `metric-provenance`, `pilot-asset-integrity`, `coding-reliability-gate`, `matrix-fingerprints`, and `A_fusion` coverage.

Exported fusion math audits use `schemaVersion: "sena-fusion-math-audit/v2"`; the explicit legacy v1 identifier remains readable for compatibility. In addition to checking `A_fusion = [alpha*S gamma*B_PC; gamma*B_CP beta*W]`, v2 verifies that layer weights and every raw, normalized, and fused S/W/B/B_PC/B_CP/G/A_fusion value are nonnegative. The audit also carries stable matrix fingerprints (`sena-stable-fnv1a32/v1`) with shapes, checksums, totals, and non-zero counts so a reviewer can confirm that the report JSON, runtime bundle, review packet, and Markdown refer to the same matrix results. These fingerprints are handoff integrity evidence, not statistical evidence.

Exported visual grammar artifacts use `schemaVersion: "sena-visual-grammar/v1"`. A visual grammar artifact records the adopted A1 Inner Solid Mesh Fusion Canvas encoding, Temporal Fusion Arc Plan-Teach-Reflect S/W/B/G encoding, and C3 Workspace Shell direction so reviewers can distinguish circular ENA concept nodes with solid internal `W` links from hexagonal SNA person nodes with outer-orbit `S` social arcs, inspect layer-relative S/W/B stroke widths and visual-salience provenance, inspect the on-canvas A1 layer key, verify readable day-mode link halos and selected-node label plates, inspect G person-code-pair totals, adjacent-window S/W/B/G/A_fusion deltas, and top G pair labels over time, and treat the temporal view as narrative inspection rather than causal evidence. The C3 shell records the ENA-inspired workspace target: left Sets/Model/Plot Tools/Stats rail, dominant central plot, right-side Primary/Secondary plot viewports, a webENA-style bottom Data View drawer for utterances, coded segments, and interactions, collapsed plot switcher, and compact Apple-style glass semantic rail icons; the Model tile uses the adopted Layer Stack glyph for model layers and fusion structure, and the Stats tile uses the adopted Network Metrics glyph combining graph nodes with metric bars. It also carries `referenceAssets` for the local A1, A2, A3, Temporal Fusion Arc, and C3 Workspace Shell mockups, with A1, Temporal Fusion Arc, and C3 Workspace Shell marked as adopted references and each reference protected by `bytes` and `sha256` fingerprints.

Exported ENA reports use `schemaVersion: "sena-ena-report/v1"`. This standalone jENA artifact isolates the epistemic layer with generated timestamps, active analysis window metadata, normalization settings, local `jena-js` runtime provenance and API surface, metric provenance, the SENA `W` concept matrix, ENA-space dimensions, unit points, code positions, connection counts, line weights, runtime-consistency handoff evidence, and interpretation guardrails.

Exported metric provenance artifacts use `schemaVersion: "sena-metric-provenance/v1"` and download as `sena-metric-provenance.json`. This standalone artifact records every reported metric's source (`sna.js`, `jena-js`, `sena-derived-from-sna.js`, `sena-self-implemented`, or `sena-composite`), scope, parity status, interpretation limit, runtime provenance, social metric snapshot, epistemic jENA snapshot, fusion S/W/B/G/A_fusion snapshot, coverage counts, and guardrails so reviewers can separate direct jSNA outputs, jENA outputs, SENA-derived metrics, and composite metrics before interpreting results.

Exported SNA and G contribution reports use `schemaVersion: "sena-sna-report/v1"` and `schemaVersion: "sena-person-code-pair-g-report/v1"`. These artifacts isolate the jSNA/sna.js social layer and the person-code-pair `G` contribution layer with generated timestamps, active analysis window metadata, parameters, local runtime provenance and API surface, metric provenance, matrices, evidence-linked reports, and interpretation guardrails.

The pilot package manifest lists both `exportArtifacts` and `exportArtifactSchemas`, so a reviewer can check each downloadable JSON/Markdown artifact against its expected schema before using it in a handoff. It also lists `assetIntegrity` fingerprints for all bundled sample/template files and `handoffChecks` for the restorable model JSON snapshot, local jENA/jSNA runtime manifests, jENA/jSNA API-surface evidence, jENA/rENA parity evidence, jSNA/R sna + igraph parity evidence, matrix-fingerprint handoff evidence, standalone jENA report, standalone runtime-consistency audit, sample/template asset integrity, single-file review packet audit, standalone coding-reliability gate, and standalone metric-provenance artifact with social, epistemic, and fusion snapshot evidence.

Exported development plans use `schemaVersion: "sena-development-plan/v1"`. A development plan records the current local-research-pilot milestone, in-scope and out-of-scope work, workflow anchors, verification gates, required artifacts, structured `runtimeParityEvidence` for jENA/rENA and jSNA/R sna + igraph fixtures, asset-integrity handoff scope, local enterprise-runtime bridge coverage, native platform-decision boundaries, and an additive `deliveryCandidate` block with the four-week pilot-delivery plan, verification commands, browser acceptance scenarios, fixed handoff package, and Chinese/English demo script.

Exported production page contracts use `schemaVersion: "sena-production-page-contract/v1"`. A production page contract records the required `/workspace/sena` text and visual smoke checks used by `npm run sena:pilot:verify`, including the six-step workflow, Model Builder controls for layout, S/W/B layer visibility, alpha/beta/gamma, threshold, and normalization, layout controls, Temporal Fusion Arc story view, jENA/jSNA runtime alignment chain, Stats rail real export controls for jSNA, jENA, G, and metric-provenance artifacts, Evidence Inspector matrix provenance, coding-reliability gate, data-governance metadata gate, claim-readiness gate, publication export controls for HTML, SVG, PNG, XLSX, DOCX, PDF, and manifest-backed packages, review-packet text, the ENA-style bottom Data View drawer under the central plot, stable Fusion Canvas SVG hooks, an unfilled violet fusion-canvas center-guide outline, weighted S/W/B link-width provenance, and the adopted A1 Fusion Canvas grammar where ENA `W` concept links render as a solid inner mesh rather than dashed lines.

Exported coding-reliability gates use `schemaVersion: "sena-coding-reliability-gate/v2"` and download as `sena-coding-reliability-gate.json`; explicit v1 identifiers remain readable as legacy evidence but are not current machine-eligible. Reliability dashboards use `sena-coding-reliability-dashboard/v2`, report unestimable Cohen kappa and Krippendorff alpha values as `null` with stable statuses, and expose a machine eligibility decision that requires every pairwise estimate and alpha to be estimable with both mean kappa and alpha at or above 0.80. Human adjudication coverage and sign-off remain external evidence and are disclosed rather than inferred. The documentation gate records the coding scheme, unit, coder count, agreement evidence, adjudication notes, limitations, and reviewer fields; PR-B claim aggregation is responsible for binding the machine result to broader claim readiness.

Group-comparison results and Holm suites use `sena-group-comparison/v2` and `sena-group-comparison-suite/v2`. Their effect-size records distinguish estimable values, insufficient samples, equal constant groups, separated constant groups, and legacy-ambiguous v1 evidence. Unestimable Cohen d and Hedges g values are serialized as `null`; zero pooled standard deviation is retained only as the observed dispersion diagnostic, never converted into a fabricated standardized effect. Explicit v1 results remain readable through compatibility normalization and are not classified as current v2 evidence.

Exported claim-readiness gates use `schemaVersion: "sena-claim-readiness-gate/v1"`. A gate records whether the current SENA output is `research-claim-ready` or `exploratory-only`, with blockers across data contract, runtime alignment, fusion math, evidence ledger, method validation, data governance, coding reliability, and human review.

Exported review packets use `schemaVersion: "sena-review-packet/v1"`. A review packet is a handoff and reproducibility-review artifact containing the report, Markdown, restorable project snapshot, standalone jENA manifest, standalone jENA report, standalone jSNA manifest, standalone runtime-consistency audit, SNA report artifact, standalone metric-provenance artifact, person-code-pair `G` report artifact, method protocol, standalone visual grammar artifact, development plan, pilot package manifest, production page contract, runtime bundle, evidence ledger, temporal trace, audits, walkthrough, verification checklist, verification compatibility audit, standalone coding-reliability gate, standalone claim-readiness gate, guardrails, local runtime dependency provenance, and `sena-review-packet-audit/v1`. The packet summary includes a structured full-conversation or temporal-window analysis scope plus coding-reliability status, and the packet audit checks that this scope is consistent across the summary, report JSON, project snapshot, runtime bundle, standalone runtime artifacts with jENA/rENA and jSNA/R sna parity handoff evidence, development plan runtime parity evidence and deferred-scope boundaries, method protocol companion artifacts, visual grammar artifact, Markdown active-window text, coding-reliability Markdown section, temporal trace sections, embedded pilot package manifest, sample/template assets, asset-integrity fingerprints, declared export artifacts, workflow export artifacts, verification required artifacts, and review-packet artifact manifest entries. Its pilot-package handoff audit also checks local runtime-manifest evidence including jENA/rENA and jSNA/R sna parity handoff markers. Its project-snapshot handoff audit checks the `model-json-export` readiness gate, graph nodes, typed edge layers, S/W/B/G, `A_fusion`, and temporal trace windows. Uploading a review packet back into `/workspace/sena` now restores editable state from the embedded `contents.projectSnapshot`; `/api/sena/projects` accepts the same review-packet handoff for server-side project creation.

Legacy snapshot compatibility normalization is a read-only projection for both file and Postgres primary state. A projection-only read/save leaves the raw persisted JSON, file-state revision, project `currentVersion`, retained project-revision `version`, and persisted project/revision snapshot hashes unchanged; an unrelated state write may advance the whole-state CAS revision, but it still preserves those snapshot hashes. In publication evidence, `persistedSnapshotSha256` identifies the raw stored snapshot, `readProjectionSnapshotSha256` identifies the non-persisted compatibility projection, and `publicationSnapshotSha256` identifies the exact rendered/exported snapshot. A difference between these hashes proves which derivation boundary was crossed; it is not evidence that the stored project was migrated. Persisting a replacement snapshot remains an explicit project update with normal project revision and audit semantics.

## Main Routes

- `/` — SENA public site shell with method, workflow, research cases, ethics, and docs sections
- `/workspace/sena` — SENA Research Pilot workspace
- `/workspace/ena` — standalone jENA-powered ENA workspace
- `/api/ena/run` — session-protected server-side jENA execution endpoint for validated ENA requests
- `/api/auth/*` — local enterprise auth with `sena-enterprise-password-policy/v1` registration/reset enforcement, failed-login audit/lockout, TOTP MFA setup/challenge verification, password reset request/confirm flow, OAuth/OIDC SSO start/callback with pilot fallback, redacted SSO provider preflight via `/api/auth/sso?status=1&preflight=1`, CSRF token issuance via `/api/auth/csrf`, session list/revoke lifecycle via `/api/auth/sessions`, and logout endpoints
- `/api/sena/projects*` — RBAC-protected project persistence APIs for SENA snapshots, review-packet handoff imports, revisions, append-only revision restore, optimistic `expectedVersion` update conflicts, collaboration state, live SSE collaboration stream, signed pub/sub bridge delivery via `POST {"action":"deliver-pubsub"}`, comments, presence, and adjudication records
- `/api/sena/snapshot/restore` — public stateless canonical validation/read-projection boundary for workspace snapshot and review-packet restore; rejects explicit cross-origin browser requests, bounds request bytes before parsing, returns stable `413 snapshot_restore_source_too_complex` for complexity/resource admission failures, returns source/projection SHA-256 evidence, and does not persist or audit the payload
- `/api/sena/docs` — machine-readable SENA API contract with route/method/auth/request/response coverage and `?format=openapi` OpenAPI 3.1 output; tests compare this manifest against the implemented Next API surface
- `/api/sena/notifications` — authenticated local in-app notification outbox for team invites, password resets, project comments, reliability reviews, validation reviews, read status, team-scoped delivery evidence, and webhook delivery/retry worker execution
- `/api/sena/provisioning` and `/api/sena/scim/v2/*` — service-token organization provisioning APIs for institution-managed team, user, SSO identity, membership sync, and SCIM 2.0 Users/Groups bridge payloads with SENA role extensions
- `/api/sena/team`, `/api/sena/team/invitations`, `/api/sena/team/memberships` — RBAC-protected team state, invitation create/accept/revoke, and membership role/status lifecycle APIs with a last-active-manager guardrail; existing signed-in users can `PATCH /api/sena/team/invitations` with `invitationId` or `inviteCode` when the invitation email matches their account
- `/api/sena/analyze` — authenticated server-side SENA analysis endpoint for project snapshots, saved projects, raw datasets, or SENA JSON contracts; can return report/snapshot/runtime-bundle artifacts, persist the result as a server project, and `GET` persisted analysis run history with report/snapshot/runtime-bundle SHA-256 fingerprints
- `/api/sena/uploads` — RBAC-protected upload registry for team source files, storing private file blobs plus original name, content type, size, SHA-256 hash, adapter profile, warning count metadata, local upload security/DLP scan status, per-upload redacted object-storage custody metadata, and native or signed-bridge object-storage delivery via `POST {"action":"deliver-object-storage"}`; add `?verify=1` to return team-scoped blob existence, checksum, missing, corrupt, and orphan-file evidence
- `/api/sena/import`, `/api/sena/reliability`, `/api/sena/validation/group-comparison`, `/api/sena/validation/expert-review`, `/api/sena/validation/claim-package`, `/api/sena/exports/publication` — enterprise import with persisted adapter/data-quality run history, `sena-import-cleaning-manifest/v1` cleaning manifests, and `POST` form `action=create-project` support that converts uploaded Excel, LMS/forum JSON, LMS/forum CSV/XLSX discussion exports, TXT/MD transcripts, and SRT/VTT subtitle transcripts into a saved team project plus persisted analysis run; the workspace upload controls now expose the same `.txt`, `.md`, `.srt`, and `.vtt` transcript entry points. Persisted multi-coder reliability run history from multipart coder files or JSON `sena-reliability-json-request/v1` annotation batches, reviewer approval/return actions, and `PATCH {"action":"adjudicate"}` batch adjudication generation from disagreement queues, persisted single or suite group-comparison validation history with permutation p values, bootstrap intervals, effect sizes, Holm correction, `sena-validation-parity-evidence/v1` manifests for rENA/R sna fixture evidence plus walkthrough/expert-review/inference gates, `sena-formal-inference-readiness/v1` checks for preregistration, study-specific model reference, runtime parity, real-data walkthrough, multiplicity control, and sample-size warnings, automatic walkthrough SHA-256 inheritance from the latest project-linked analysis run when `projectId` is supplied, persisted domain expert review sign-off with claim-scope/rating evidence, reviewer approval/return actions, project-scoped `sena-enterprise-claim-evidence-package/v2` aggregation of approved reliability/validation/preregistration/parity/expert-review evidence, and publication export APIs. The enterprise publication route requires `projectId`, rejects inline snapshots with a stable `400`, returns stable `413 publication_export_derivation_too_complex` when request-wide derivation admission exceeds the work ceiling, and preserves RBAC and audit evidence; if the persisted report is missing machine evidence, a current project-bound v2 reliability run can produce an export-only derived snapshot without changing the project. The synchronous resolver now reads primary state once and emits `sena-publication-state-binding/v2`, binding the project version, raw persisted snapshot hash, current-v2 read-projection hash, exact claim-package hash, approved reliability-run hash, adjudication-coverage hash, and file/Postgres state revision before deriving the publication snapshot. `publicationDerivation`, `x-sena-publication-reliability-run-id`, and separate persisted/read-projection/publication snapshot hashes preserve each evidence boundary. Incomplete reports still fail the model-card render gate. Publication outputs include SVG, PNG, XLSX workbooks with claim-readiness/coding-reliability/data-governance/matrix-fingerprint/evidence-snippet sheets, DOCX, PDF, `sena-publication-package/v1`, `sena-publication-source-snapshot/v1`, `sena-data-governance-metadata/v1`, and `sena-publication-verification-certificate/v1` artifacts
- `/api/sena/governance/health` and `/api/sena/governance/security` — authenticated enterprise runtime health, persistence, RBAC, audit, collaboration stream/pub-sub delivery, notification/email delivery, backup/restore-rehearsal, and a dedicated `sena-enterprise-security-posture/v1` security review artifact for identity, access, data protection, audit/monitoring, and continuity controls
- `/api/sena/governance/audit` — authenticated `team:manage` audit-log query/export endpoint with team, event, user, project, date, pagination, JSON, CSV, `?integrity=1` SHA-256 linked audit-chain/retention evidence, and signed audit/SIEM webhook delivery worker via `POST`
- `/api/sena/governance/backup` — authenticated `team:manage` backup export, verification, signed managed-backup webhook delivery, signed managed-database sync bridge delivery via `POST {"action":"sync-database"}`, dry-run restore, and merge restore with payload SHA-256, record counts, secret exclusions, and ID collision preflight
- `/api/sena/ops/status`, `/api/sena/ops/metrics`, `/api/sena/ops/readiness`, `/api/sena/ops/jobs`, `/api/sena/ops/jobs/worker-contract`, `/api/sena/ops/deployment`, `/api/sena/ops/native-adapters`, `/api/sena/ops/saas-operations`, `/api/sena/ops/go-live-rehearsal`, `/api/sena/ops/platform-decisions`, `/api/sena/ops/release-gate`, `/api/sena/ops/alerts` — bearer-token or session-protected deployment status, Prometheus-style runtime metrics, production-gate readiness evidence, server job queue status/retry/DLQ worker callbacks, external worker contract and heartbeat evidence, redacted organization deployment handoff package with `sena-enterprise-platform-decision-register/v1` plus native adapter certification, SaaS operations readiness, go-live rehearsal, latest release-gate verification evidence, team-RBAC platform decision acceptance and release-gate review records, machine-readable firing alerts, and signed alert webhook delivery via `POST {"action":"deliver"}` for storage, backup freshness, upload integrity, collaboration pub/sub queues, database-sync configuration, record counts, SSO/provisioning/notification/email configuration, alerting ownership, and platform-decision blockers
- `/api/sena/ops/production-evidence` — bearer-token or session-protected `sena-enterprise-production-evidence-manifest/v1` export that aggregates artifact custody for the Vercel production preflight, Postgres, object storage, CDN, server job queue, external worker contract, observability, the production performance budget, and the conference load rehearsal
- `/api/sena/ops/capability-audit` — bearer-token or session-protected `sena-enterprise-capability-audit/v1` export mapping login/SSO, RBAC collaboration, server persistence, backend APIs, import adapters, multi-coder reliability, validation inference, publication exports, production governance, and go-live operations to runnable evidence plus remaining platform decisions
- `/api/sena/ops/go-live-rehearsal` — bearer-token or session-protected `sena-enterprise-go-live-rehearsal/v1` export with `sena-enterprise-release-gate-draft/v1` release-gate prefill evidence, `?artifact=rollback-drill` standalone `sena-enterprise-go-live-rollback-drill/v1` backup/restore/incident runbook evidence, and `?artifact=post-cutover-monitor` standalone `sena-enterprise-go-live-monitor/v1` plus latest `sena-enterprise-post-cutover-observations/v1` evidence for the workspace approval form
- `POST /api/sena/ops/go-live-rehearsal` — team-RBAC `sena-enterprise-go-live-attestation/v1` cutover attestation record with checklist evidence and blocked-approval guardrails, or action bodies to start/sample/complete the required 60-minute `sena-enterprise-post-cutover-observation/v1`
- `/login`, `/register`, and `/reset-password` — runnable local enterprise auth entrypoints

## Verification

Run:

```bash
npm run sena:pilot:verify
npm run sena:go-live:check
```

Self-managed go-live closeout sequence:

1. Run `npm run sena:pilot:verify`.
2. Run `npm run sena:self-managed:workflow` to create the self-managed release, backup, restore, audit, release-gate, rollback, and active post-cutover observation evidence.
3. Run `npm run sena:post-cutover:observe -- --watch --attest` to sample the real 60-minute observation window, complete it after `requiredUntil`, and submit the self-managed approved go-live attestation.
4. Run `npm run sena:go-live:check`.

Equivalent manual sequence:

1. Start the post-cutover observation with `POST /api/sena/ops/go-live-rehearsal` and `action=start-post-cutover-observation`.
2. Record samples through the 60-minute window with `action=record-post-cutover-sample`; samples must include the start and a final at/after `requiredUntil`, and intermediate gaps cannot exceed seven minutes.
3. Complete the observation with `action=complete-post-cutover-observation`, acknowledging any warning alerts with owner/runbook evidence.
4. Submit the approved go-live attestation after `postCutoverMonitor.status` is `ready`.

Latest local verification note: `../notes/session-logs/2026-06-27_sena_handoff_risk_closeout.md` records the 2026-06-27 dirty-worktree, release-gate, and go-live status. The full `npm run sena:pilot:verify` gate passed after the local port-3005 SENA server was stopped. A later self-managed closeout run refreshed backup/release evidence, completed the real 60-minute post-cutover observation with an approved attestation, and `npm run sena:go-live:check` exited successfully at `2026-06-27T16:59:26.260Z` with overall `status: "ready"`. The 2026-06-17 full local gate in `../notes/session-logs/2026-06-17_sena_enterprise_identity_local_verification.md` remains historical evidence, not the current handoff baseline. Institution-owned IdP, SSO secret custody/rotation, SCIM/IdP lifecycle evidence, managed infrastructure, and staffed operations must still come from the platform owner before institution-owned production cutover. The platform-owner request packet is `../exports/session-outputs/2026-06-17_sena_identity_platform_owner_request_packet.md`.

That command checks that this project's local Next.js dev/start processes are clear, removes the generated `.next` directory, and then runs the full local pilot gate. It verifies that the `/workspace/sena` production server artifacts and manifests exist after build, starts a temporary `next start` server on an available local port, requests `/workspace/sena`, checks the built page against `sena-production-page-contract/v1`, runs a Playwright browser smoke over the demo workflow, and then stops the temporary server. The browser smoke fetches the Data Import pilot package manifest, sample assets, and blank templates from the visible page links; downloads and re-uploads the generated empty contract template; uploads the lesson-study JSON contract; uploads the five-table lesson-study CSV sample; loads the built-in lesson-study sample; switches Fusion Canvas layouts; adjusts alpha/beta/gamma; changes temporal modes; inspects SVG evidence; parses key JSON/Markdown downloads; restores the workspace from a re-uploaded review packet; and restores the workspace from a re-uploaded project snapshot. This avoids stale or half-written Next.js manifests, because `next build` rewrites `.next` and should not run while `npm run start` or `npm run dev` is serving the same workspace. Set `SENA_VERIFY_SMOKE_PORT` to choose the starting port for the temporary production smoke test.

Equivalent manual sequence:

```bash
npm run sena:pilot:smoke
npm run sena:pilot:browser-smoke
npm test
npm run build
npm run sena:performance:check
SENA_CDN_VERIFY_URL=https://your-cdn.example/workspace/sena npm run sena:cdn:verify
```

Expected current baseline after the research pilot package work:

- Pilot smoke passes for lesson-study import, jENA/jSNA manifests, standalone jENA report, audits, runtime bundle, demo verification, and Markdown report export
- Data Import exposes stable pilot asset links for the package manifest, six lesson-study sample files, six blank templates, asset-integrity fingerprints, and manifest-aligned handoff checks; browser smoke fetches those links, checks them against `sena-pilot-package-manifest/v1`, verifies the fingerprint signal and handoff checks, and verifies that the generated `Contract template` download can be re-uploaded as an empty SENA dataset
- Method Validation shows visible metric provenance for direct jSNA, jENA, SENA-derived, SENA-implemented, and SENA composite metrics, including R/rENA parity fixture status and interpretation limits.
- Runtime parity and artifact evidence remain available through exported runtime bundle, runtime consistency audit, jENA report, jSNA manifest, and review-packet artifacts.
- Report Generator shows the demo verification checklist with automated evidence, sample-import asset-integrity evidence, editable manual status/reviewer/notes, pending/passed/failed manual counts, visible compatibility gate status, visible production page contract, coding-reliability gate, data-governance metadata gate, claim-readiness gate, review-packet audit with project-snapshot, development-plan, and method-protocol handoff checks, required artifacts, `sena-demo-verification-compatibility-audit.json`, and `sena-review-packet.json`; report completeness audits include explicit full-conversation or active-window analysis-scope, coding-reliability, and data-governance items
- Review packet export contains report Markdown, report JSON with claim-readiness, coding-reliability, and data-governance gates, standalone coding-reliability gate, standalone claim-readiness gate, standalone jENA report, method protocol, visual grammar artifact, development plan, pilot package manifest, production page contract, runtime bundle, evidence ledger, temporal trace, audits, walkthrough, verification checklist, verification compatibility audit, local runtime dependency provenance, asset-integrity and method-protocol handoff checks, and `sena-review-packet-audit/v1`
- Vitest passes for ENA and SENA model/import/report coverage
- Next production build succeeds
- Next production artifacts exist for `/workspace/sena`, including server page output, client-reference manifest, static HTML/RSC output, and error-page manifests
- Temporary production server smoke reaches `/workspace/sena` and sees the six-step workflow, layout controls, jENA/jSNA runtime panel, key export controls, compatibility-audit text, review-packet audit text, `sena-review-packet.json` export text, stable Fusion Canvas SVG hooks, and an unfilled violet Fusion Canvas center-guide outline
- Browser smoke downloads and schema-checks the main declared research artifacts, including runtime bundle artifact evidence coverage for jENA/jSNA, matrix-fingerprint handoff coverage, pilot asset integrity, standalone metric-provenance artifact, standalone coding-reliability gate, data-contract audit, temporal runtime trace, evidence ledger, walkthrough, verification, compatibility audit, production page contract, development plan, pilot-readiness audit, review packet with archived method protocol, visual grammar, and fusion math audit records, report JSON/Markdown with coding-reliability gate, jENA manifest, standalone jENA report, jSNA manifest, SNA report, and G report
- `/workspace/sena` is statically generated

## File-state lock recovery

The local file-state lock never auto-deletes a lock that merely appears stale: it fails closed on timeout to avoid deleting a replacement writer's lock. After a hard process exit, an operator must first verify that no process owns the exact lock before explicitly recovering it; multi-process deployments should use the managed Postgres state path.

## Runtime Provenance

SENA uses local JavaScript analysis packages:

- `packages/sena-kernel` is the private `@sena/kernel` transition package for M2-M8 analytical functions. It re-exports the current tested TypeScript core while the modules are decomposed, and `/api/sena/analyze` consumes this boundary for server-side analysis runs.
- `jena-js` is the browser/Node TypeScript ENA engine used by `/workspace/ena` and SENA report manifests.
- `sna.js` is the browser/Node social-network analysis engine used by `/workspace/sena` social metrics.
- Runtime versions, local `file:vendor/...` dependency specs, concrete API surfaces, and development-time parity evidence are read into provenance and propagated into manifests, standalone jENA/SNA/G reports, temporal traces, runtime bundles, method protocols, review packets, Markdown reports, and enterprise validation parity evidence. Validation parity evidence also records the project-linked analysis run or project snapshot SHA-256 used as the real-data walkthrough anchor. The current API surface records jENA `ena()` and jSNA/sna.js `gden()`, `nties()`, `degree()`, `components()`, `isConnected()`, and `geodist()`. The jENA parity evidence records the bundled rENA fixture at `lib/ena/__fixtures__/r-ena-sample-parity.json`, covering line weights, connection counts, variance, unit points, and node positions. The jSNA parity evidence records the bundled R sna + igraph fixture at `lib/sena/__fixtures__/r-sna-social-parity.json`, covering degree, weighted degree, betweenness, closeness, reachable nodes, reciprocity, average path length, components, and communities. These fixtures do not require a live R runtime in the website.
- Runtime consistency audits check local package dependency provenance, recorded API surfaces, jENA adjacency keys and connection-count signal overlap against SENA `W`, and jSNA social graph and S matrix outputs against the SENA social layer. The same audit can be downloaded as `sena-runtime-consistency-audit.json`.
- The website does not run a live R process. R-derived fixtures are used for parity testing and method validation where available.

## Interpretation Guardrails

- `A_fusion` is a normalized typed adjacency model, not a causal model.
- `B` aliases `B_PC`; `B_CP` uses independently declared target-person evidence when available and otherwise uses the audited `B_PC`-transpose fallback. Independent `B_CP` makes `A_fusion` directed and requires directed analysis or declared symmetrization.
- Explanatory and ENA-space layouts use position for readability.
- Joint mode uses declared `A_fusion` embedding operators: Laplacian eigenmaps, MDS + Schoenberg, or commute-time; distance claims must carry operator, delta, dimension, deterministic seed, metric exactness, and stress.
- Human review and coding-reliability documentation are required before using exported interpretations for publication, assessment, or instructional decisions.
